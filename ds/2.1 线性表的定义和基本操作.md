
![[截屏2026-03-14 20.52.41.png]] 🧚‍♀️顺序和链式；数组是顺序表，list 是链表![[截屏2026-03-14 20.56.17.png]]
数据类型必须相同，可以是自己定义的结构类型struct A ，可以将其作为数据元素的数据类型，有限有次序（不能是无限的，不能是无序的）![[截屏2026-03-14 20.59.35.png]] ![[截屏2026-03-14 21.00.23.png]] 当数据元素当中包含多个数据项的时候类似于一张表![[截屏2026-03-14 21.01.28.png]]
```cpp
//初始化线性表中的顺序表
struct SqList{
	int* data;
	int length;//需要一个记录现有长度的变量
	int capacity;
}
void initList(SqList &L,int c){
	L.capacity = c;
	L.data = new int[L.capacity];
	L.length = 0;//刚刚建好的表长度是0
}
int main(){
	
}
```