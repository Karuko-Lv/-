![[截屏2026-05-11 20.54.03.png]]
![[截屏2026-05-11 17.21.45.png]]
![[截屏2026-05-11 22.33.54.png]]
## 权相关的重要概念
![[截屏2026-05-11 17.29.04.png]]
### 结点的权
给树里的各个结点附上权值
### 结点的带权路径长度
从**树的根结点**到指定一个结点的路径的长度（即经过的边树）✖️结点的权值
### 树的带权路径长度
树中所有的叶子结点的带权路径长度之和 WPL
==>只需要计算所有的叶子结点即可

## 哈夫曼树
### 哈夫曼树定义
![[截屏2026-05-11 17.35.22.png]]
定义：给定 n 个带权叶子结点，构造含有这 n 个叶子结点的所有二叉树当中，带权路径最小的二叉树称之为“哈夫曼树”
「PS」中间这两棵树就是哈夫曼树，包含着四个带权叶结点的哈夫曼树**还可能出现其他形态**

### 哈夫曼树构造
[[03:40哈夫曼树的构造方法]]
Q：给定几个叶子结点怎么构造包含这几个叶子结点的哈夫曼树???
🧚‍♀️边最短的用权重最大的，边最长的用权重最小的？
A：
1️⃣每一次从结点集合中选择两个权值最小的结点使其成为兄弟，将这两个权值之和作为新树的根结点的权值
2️⃣再从更新后的集合再选择两个根结点权值最小的两棵树让其成为兄弟
......
![[截屏2026-05-11 17.56.02.png]]
🧚‍♀️贪心算法＋新节点的插入排序；看不懂的建议先学一下哈夫曼编码；这不对吧，1 与 2 新生成的节点 3 不应该与 2 结合，应该是 2 与没有生成的 3 结合，这样深度才更低，2 与 3 生成的 5 再与 1 与 2 生成的 3 结合，这样才最小吧
### 哈夫曼树的性质
根据过程知道哈夫曼树的性质
![[截屏2026-05-11 18.00.59.png]]
1️⃣刚开始的初始结点最终都会成为叶子结点，且权值最小的结点到根结点的距离越长
2️⃣由于每次让两个树进行结合，总共 $n$ 个结点两两结合合并成一棵树总共需要合并 $n-1$ 次。且每一次合并都会导致增加一个分支结点，所以哈夫曼树结点总数 $2 n-1$
3️⃣哈夫曼树中~={red}不存在度为 1 的结点=~
4️⃣哈夫曼树并不唯一，但是 WPL 必然相同且为最优
![[截屏2026-05-11 18.00.47.png]]
6️⃣度为 $m$ 的哈夫曼树只有度为 $m$ 和度为 $0$的结点，且度为 $m$ 的结点数=度为 $0$ 的结点数$-1$
7️⃣有 $n$ 个叶结点的哈夫曼树一共有 $2 n-1$ 个结点
### 哈夫曼树的经典应用：哈夫曼编码
定义：哈夫曼编码即用构造哈夫曼树的方法来确定的一种字符集的编码方案，字符集当中的每个字符都要作为一个叶子结点，同时需要将各个字符出现的频度作为这个结点的权值，可以将哈夫曼树的左指针看成 0 右指针看成 1（反过来右 0 左 1也无所谓）；由于给定的叶子结点构造的哈夫曼树不唯一所以哈夫曼编码这种编码方案也是不唯一的，但是 WPL 都是 130，即无论用哪种方案最后都只需要传送 130 bit（130 个二进制位） 即可将 100 道选择题全部传过去==>哈夫曼编码可以用于**~={red}数据的压缩=~**
![[截屏2026-05-11 20.48.01.png]]
发电报就需要用到哈夫曼编码，哈夫曼编码的构造是基于哈夫曼树的原理，比较短的 di 声和长的 di 声用于区分两种信号
接受电报或者窃听电报的人只需要将点和划的信息翻译成有意义的字符即可
Q：对于一个给定的字符集怎么设计一个优秀的编码方案？
A：
![[截屏2026-05-11 18.05.17.png]]
Q：思考有没有更优秀的编码方案？即传递的二进制比特信息尽可能少，即尽可能追求最终构造的编码树的带权路径长度尽可能小
**~={red}可变长度编码=~**（各个字符所对应的二进制长度不相同 ）
![[截屏2026-05-11 18.07.27.png]]
Q：为什么不能将 A 只用一个二进制位来表示呢
A：可以解释成不同的答案
![[截屏2026-05-11 18.10.59.png]]
🧚‍♀️非叶子结点答案不唯一吧；最短前缀不同；解码解不出来；有二义性了；信息论；短操作码不能是长操作码前缀；就是要四个互相没有交集；CRC 循环校验码（bushi）；woc 拓展操作码；为什么不叫“非前缀编码”
「总结」对于一个字符集如果要设计一个可变长度编码，所有字符集当中的字符对应到编码数中只能当叶子结点!!! 不可以当某一个分支结点
即**前缀编码**：其中任何一个字符的编码都不是另一个编码的前缀
#### 英文字母频次压缩 

![[截屏2026-05-11 20.50.10.png]]

「考研常考‼️」对于给定的字符集如何设计一个哈夫曼编码==>本质是构造一棵哈夫曼树
🧚‍♀️哈夫曼编码后使用频率越高的字符所用的编码越少
## 并查集
2022 新增考点
![[截屏2026-05-11 22.33.54.png]]
![[截屏2026-05-11 20.55.46.png]]
### 逻辑结构
本质上是**集合类**的逻辑结构
在集合这种逻辑关系下，将各个元素划分为若干个互不相交的子集，任意挑选出两个元素，他们两要么是从属于同一个集合 or 从属于不同集合

Q：怎么用代码表示集合的关系？
![[截屏2026-05-11 21.20.57.png]]
A：森林中 m 棵**互不相交**的树，所以互不相交的集合关系可以用树、森林的方式表示，即属于同一个集合在物理上组织成一个树状的结构，不同集合放在不同树中
-->用互不相交的树表示多个集合
🧚‍♀️直接结构体定义一个变量记录一下不就好了；根节点不同就是不同的集合

「最终目的」使用集合这种数据结构
Q：怎么使用集合这种数据结构，有哪些需求？
A：
1️⃣给一个指定的元素 L，判断 L 属于哪一个集合，即找到元素所属的集合
2️⃣指定两个元素 L 和 H，判断这两个元素是否从属于同一个集合
==>~={red}“查”操作=~
Q 1 如何“查”到一个元素到底属于哪一个集合？
A：找根
![[截屏2026-05-11 21.27.54.png]]
🧚‍♀️所以是后序遍历喽
3️⃣将两个集合合“并”为一个集合
==>~={red}"并"操作=~
Q 2 如何让两个集合即两棵树“并”
A：只需要让其中一棵树成为另一棵树的孩子即可
![[截屏2026-05-11 21.30.15.png]] 🧚‍♀️数组合并时间复杂度为 O (n)
### 物理存储
![[截屏2026-05-11 21.33.37.png]]
并查集非常适合用双亲表示法，因为“并“和“查“都是需要找到父结点，“并”找到父结点之后将其中一棵树的根结点指向另一棵树的根结点
🧚‍♀️有点像迪杰斯特拉算法找最短路径
Q：对于 n 个数据元素如何表示数据元素之间的集合关系？
A：只需要声明一个长度为 n 的 int 型数组，用数组表示集合关系，本质上是树的双亲表示法
![[截屏2026-05-11 21.47.34.png]]
#### 并和查
![[截屏2026-05-11 21.50.34.png]]
e.g A 和 C 并起来即将C 所对应的数组的值指向 A 的下标即 0 即可
🧚‍♀️不会是力扣 71 吧，那个太小众了
#### “并查集”的代码实现
##### 初始化
```cpp
#define SIZE 13
int UFSets[SIZE];//集合元素数组
//初始化并查集
void Initial(int S[]){
	for(int i=0;i<SIZE;i++){
		S[i]=-1;
	}
}
```
1️⃣用数组表示并查集
2️⃣将数组的元素初始化为-1 表示各个元素之间互不相交，每个元素是独立的子集
![[截屏2026-05-11 21.55.16.png]]
初始化之后再根据实际的业务需求对各个元素进行一个合并
🧚‍♀️C 语言数组本质就是指针，数组作为参数就是传递数组的指针；并查集没有拆操作

##### 并和查具体代码实现
###### 查
```cpp
//Find "查"操作，找到x所属集合（返回x所属根结点）
int Find(int S[],int x){
	while(S[x]>=0)//寻找x的根
		x=S[x];
	return x;//根的s[]值小于0
}
```
🧚‍♀️这不是变相的递归吗；s[x]内 x 是下标，一开始传就是下标，s[x]=是双亲结点，没有地方放元素本身；那我想问怎么存数据元素呢？这里只有下表和位置的映射关系啊；这咋和 next 数组那里有点像，有人和我一样感觉吗？；有点 nextval 的感觉；跟 KMP 优化思想一样的
###### 并
```cpp
//Union“并”操作 
void Union(int S[],int Root1,int Root2){//传入两个集合的根结点
	if(Root1==Root2) return;
	S[Root2]=Root1;
}
```
🧚‍♀️路径压缩不是更方便；我觉得应该先 find 到跟节点 `s[root2]=root1;   p[find[s1]]=s2;`
🧚‍♀️没错，Root 要求的就是根节点的数组下标，如果两个相等会导致写成自己指自己出错。`S[Root 1]恒等 S[Root 2]==0`，你这判断条件才是有问题的。

如果给定两个元素（非根结点），将这两个元素所从属的集合合并
1️⃣先查找到根
2️⃣再 Union
###### 时间复杂度+并的优化
![[截屏2026-05-11 22.08.47.png]]

因为 `Find` 操作在树很高的情况下复杂度高，所以对 `Union` 操作进行优化
优化思路：在每次 Union 操作构建树的时候尽可能让树不长高，即让 union 每次都保证小树合并到大树底下使得树的高度的增加没有很快
![[截屏2026-05-11 22.24.23.png|200]]
Q：如何表示树的大小？
A：用根结点的绝对值即可
![[截屏2026-05-11 22.29.39.png]]
🧚‍♀️王道书上的查操作时间复杂度为 O (d)，d 为树深度；这 Union 时间复杂度是个大坑，后面 n 平方的公式完全对不上；Balance Plus Tree；上红黑树; No 万一高度不一样呢；那小树也不一定会更矮呀；其实就是小树复合到大树里面查找根的次数少；小树大树的区别是节点数量吧，不是树的高度吧？这怎么弄啊；因为并查集的本质是集合吧；集合里面的节点最开始就是这样的并方法，不会存在节点数小的树更高的情况
![[截屏2026-05-11 22.30.45.png]]
🧚‍♀️数学归纳法就是递归啊
![[截屏2026-05-11 22.32.56.png]]
PS：优化之后并查集的整体效率会提升-->并任然是 `O(1)`，查是 `O(log2n)`

### 并查集的终极优化——Find 操作
小树并入大树是基础优化思路，可以保证并查集构造的树的高度不超过 `O(log2n)

之前的 `Find` 操作是从指定结点出发依据数组信息一路向上找到其所属的根结点

优化——压缩路径——使得查找路径变得更短
🧚‍♀️递归压缩；盲猜和 kmp 算法优化差不多