AA🧚‍♀️传说中的流水线，袁老师书用了 40 多页，可见重要性

## 习题

## 概述
![[截屏2026-07-05 17.12.58.png]]
### MIPS 32 指令流水线
![[截屏2026-07-05 17.16.04.png]]
🧚‍♀️instruction fetch, instruction decode, execute, memory, write back ![[截屏2026-07-05 17.16.26.png]]  为简单起见，假设指令执行过程中**各功能段的时间延迟均为 T**
在第一个时间段 T 中指令 1 在取指功能部件中处理；
在第二个时间段 T 中指令 1 在译码功能部件中处理；
在第三个时间段 T 中指令 1 在执行功能部件中处理；
在第四个时间段 T 中指令 1 在访存功能部件中处理；
在第五个时间段 T 中指令 1 在写回功能部件中处理；
在第五个时间段结束时指令 1 执行完毕，之后指令 2 依次进入上述五个功能段进行处理![[截屏2026-07-05 17.23.04.png]]
为提高指令的吞吐率，可想办法使各功能部件可以并行工作，实现如图所示的指令流水线效果![[截屏2026-07-05 17.25.52.png]]
在第五个时间段 T 中各功能部件中都有 1 条指令在处理，即各功能部件在并行工作，流水线被充满，也称之为满载
第五个时间段 T 结束后指令 1 执行完毕，之后每经过一个时间段 T 流水线将完成 1条指令的执行
🧚‍♀️滑动窗口？
Q：怎么修改单周期 MIPS CPU 逻辑架构实现这样的指令流水线呢
A：在每个功能段后面增加一个流水寄存器![[截屏2026-07-05 17.27.14.png]]
![[截屏2026-07-05 17.28.39.png]] 🧚‍♀️27 王道 248；存储器那章有个交叉编制；那要是发生中断怎么办？
🧑‍🏫经过以上修改，指令流水线时空图的横坐标中**各时间段 T 变成了 CPU 时钟周期**；之前单周期非流水线 MIPS CPU逻辑架构的 CPU 时钟周期由 5 个时间段 T 构成
![[Pasted image 20260705175455.png]]
实际上各功能段时间延迟不一样，将各功能段中最大关键延迟记为 $T_{max}$，并将其作为指令流水线的时钟周期
执行 1 条指令所需的时间为 5 个时钟周期；
执行 2 条指令所需的时间为 6 个时钟周期；
...
针对 **5 级流水线**：执行 $n$ 条指令所需要的时间为 $n+4$ 个时钟周期![[截屏2026-07-05 17.53.19.png]]
理解：第 1 条指令需要经过 m 个时钟周期，之后每个时钟周期完成 1条指令的执行
🧚‍♀️408 计算机组成原理中，CPU 时钟周期和时钟周期通常可以认为是同一个东西；流水线 CPU 不能依赖单总线数据通路；交叉存储器；![[截屏2026-07-05 17.56.21.png]]
🧚‍♀️下面的 tmax，这是在已经稳定的过程中，还有非流水线执行一条指令所需的时间不是 5 倍的最大延迟，应是五段延迟加起来，小于等于 5 倍的最大延迟；只是单条指令的话，非流水线技术的执行时间会小于等于下面的，因为流水线必须要求时钟周期是最大时延，单条指令要五个![[截屏2026-07-05 17.56.46.png]]
PS：计算机网络中也学习过**流水线思想**，例如分组交换网中的分组交换机（路由器）在转发先前收到的分组的同时还在接收下一个分组，进而形成了流水线式的分组传输
![[截屏2026-07-05 17.58.30.png]]
又例如相较于“停止-等待协议”，“后退 N 帧协议 GBN”和“选择重传协议 SR”都采用流水线传输方式，进而显著提高信道利用率
🧚‍♀️TCP 滑动窗口？；分组转发；跟计组第三章多体并行存储器的思想也很想
### 习题
![[截屏2026-07-05 17.59.50.png]]
A ![[截屏2026-07-05 18.01.07.png]]
忽略各功能段之间的缓存时间就是忽略各流水寄存器带来的时间开销，故各功能段中最大关键延迟是 CPU 时钟周期
![[截屏2026-07-05 18.01.45.png]]
C ![[截屏2026-07-05 18.06.56.png]]
流水线吞吐量即单位时间内可完成的指令数量
🧚‍♀️这里的吞吐量就是 MIPS 里的 IPS 计算公式
![[截屏2026-07-05 18.07.36.png]] D ![[截屏2026-07-05 18.09.16.png]]
🧚‍♀️把 80 那个放到最后一个不就不需要流水寄存器了吗；我认为图画错了最后一个也需要流水寄存器 
## 流水线数据通路
🧚‍♀️27 王道没有，就 250 页勉强有个指令流水段，指令还不全一样
![[截屏2026-07-05 18.12.08.png]]
MIPS 32 标准 5 段流水线的进一步细化，是**由单周期 MIPS 处理器的专用数据通路**改造成的流水线架构
🧚‍♀️27 王道 210 页，高老师的图第 1 部分对应取指令部件设计，第 23 部分是 I 型指令运算部件的数据通路，第 45 部分是访存指令的数据通络，完整结构图建议两边结合着看互相补充细节；27 王道还有 P 249 ![[截屏2026-07-05 18.16.52.png]] ![[截屏2026-07-05 18.18.24.png]]

采用单周期 MIPS 处理器数据通路改造的流水线架构与单周期处理器使用相同的操作控制信号，所以**操作控制器可以复用**（图中用蓝色表示）
指令流水线中各个功能段之间的流水寄存器传递：
1️⃣当前**指令**
2️⃣当前阶段代加工**数据**
3️⃣后续功能段数据架构所需要的操作**控制信号**
由于指令流水线中的**译码功能段**负责指令译码，并生成相应的操作控制信号->操作控制器应该设置在译码功能段中，**操作控制器用于控制数据通路**但是不属于数据通路
每一条指令的**所有控制信号**在**译码阶段**由**操作控制器一次产生**，逐功能段使用
‼️每个操作控制信号只会在一个功能段使用一次，使用完后**不再向后续功能段继续传递**![[截屏2026-07-05 20.50.09.png]]
例如图中的控制信号 `RegDst` 在译码功能段使用，故没有通过译码功能段与执行功能段之间的流水寄存器传递到执行功能段，而其他控制信号都传递到了执行功能段
在传递到执行功能段的控制信号中，`Branch`、`AluOp`、`AluSrc` 这三个控制信号在执行功能段使用，因此没有通过执行功能段与访存功能段之间的流水寄存器传递到访存功能段，而其他 3个控制信号都传递到了访存功能段
在传递到访存功能段的控制信号中，`MemWrite` 控制信号在访存功能段使用，因此没有通过访存功能段与写回功能段之间的流水寄存器传递到写回功能段，而其他 2个控制信号都传递到了写回功能段
 在传递到写回功能段的控制信号中，`MemToReg` 控制信号在写回功能段使用，而 `RegWrite` 控制信号会反馈到译码功能段中的寄存器组的**写入控制端**
 另外传递到执行功能段的 `Branch` 控制信号与 ALU 的运算标志信号进行逻辑与操作，得到分支跳转控制信号后反馈到取址功能段段使用, 以便进行分支处理
  ![[截屏2026-07-05 20.51.31.png|200]]
  ![[截屏2026-07-05 20.52.03.png]]
 
![[截屏2026-07-05 20.53.19.png]]
🧚‍♀️写回指的是写入寄存器而不是内存；这里必考务必完全掌握; 按照近几年计组命题风格，**流水线的数据通路还真有可能考**。袁书上也有详细介绍
🧑‍🏫下面以 5 条指令顺序进入上图所示流水线架构为例，分析指令在流水线中的执行过程：
1️⃣ `lw` 指令在时钟节拍的控制下进入取址功能段：即通过**程序计数器 PC** 给 ~={blue}**指令存储器=~ ~={orange}提供 `lw` 指令的存储地址=~**，进而**从~={blue}指令存储器=~中取出 `lw` 指令**并送到取指功能段与译码功能段之间的流水寄存器的输入端；另外 PC 的值会送到取指功能段中的加法器与 4 相加形成顺序指令地址 `PC+4`，`PC+4` 会传送到取值功能段中的用于为 PC 选择下一条指令地址的多路选择器的其中一个输入端、还会送到取指功能段与译码功能段之间的流水寄存器的输入端
🧚‍♀️这里为什么不是根据 pc 的内容从主存里面取出指令放进 IR 里面啊[chatgpt]取指是从主存中取的，取指 = 从主存把指令取到 IR
![[截屏2026-07-05 21.27.32.png|200]]
2️⃣在下一个时钟节拍的控制下进行的相关操作：
取指功能段与译码功能段之间的流水寄存器的**各输入端的数据**`lw` 和 `PC+4` 被锁存到该流水寄存器中，以便在译码功能段中使用或传递给后续功能段
`lw` 指令进入到了译码功能段进行处理，而 `sw` 指令进入到了取指功能段进行处理
为了更好理解 `lw` 指令在译码功能段的处理，回忆 `lw` 的格式和执行的操作
![[截屏2026-07-05 21.28.34.png]]
锁存在流水控制器的 `lw` 指令的操作码字段被送入控制器，以便由控制器产生该指令在各功能段所需的操作控制信号
其中**目的寄存器选择控制信号**`RegDst` 就在当前的译码功能段使用，其他控制信号被送入译码功能段与执行功能段之间的流水寄存器的输入端； 
目的寄存器选择控制信号取值为 0，以便控制译码功能段中多路选择器，将该指令中的目的寄存器的编号进行选择输出，进而送到译码功能段与执行功能段之间的流水寄存器的输入端，将目的寄存器的编号记为小写的 rt；
同时指令中的 rs 寄存器的编号和 rt 寄存器的编号被送入到译码功能段中的寄存器组，以便读出相应 rs 寄存器和 rt 寄存器各自的内容，进而送到译码功能段与执行功能段之间的流水寄存器的输入端，用 `RS`、`RT` 表示
Q：`lw` 指令并不需要读取 rt 寄存器的内容，而是从主存中读取内容并写回到 rt 寄存器，why 这里要读取 rt 寄存器内容
A：🧑‍🏫指令流水线中每个功能段的设计并不仅仅为了某一条指令，要满足指令集中所有指令。For 译码功能段主要是 ⓵译码并产生指令在各功能段所需的控制信号，⓶读取寄存器组中指定寄存器的内容，⓷还要将指令中 16 位立即数进行符号扩展，扩展为 32 位立即数 ⓸将上述控制信号和数据送到译码功能段与执行功能段之间的流水寄存器的输入端
PS：另外之前所存在前一级流水寄存器中的 PC+4 的值也会送到译码功能段与执行功能段之间的流水寄存器以便后续传递到执行功能段中计算分支跳转地址（本节课不分析分支跳转的情况，在介绍指令流水线**控制冲突**会在这幅图进行分析）
🧚‍♀️ sw 会用到 RT
![[截屏2026-07-05 21.49.21.png|300]]
3️⃣在下一个时钟节拍的控制下进行的相关操作：
译码功能段与执行功能段之间的流水寄存器的**各输入端的控制信号及数据**被锁存到译码功能段与执行功能段之间的流水寄存器，以便在执行功能段使用或传递给后续功能段
此时 `lw` 指令进入到了执行功能段进行处理，其后的 `sw` 指令和 `beq` 指令分别进入到了译码功能段和取指功能段进行处理
`lw` 在执行功能段处理如下：
在传递到执行功能段的控制信号中在**不考虑分支跳转的情况**下只有 `AluOp`、`AluSrc` 这两个控制信号在执行功能段使用
1. 目的寄存器 rt 被传送到下一级流水寄存器的输入端
2. RS 寄存器的内容被传送到算术逻辑单元 ALU 的输入端 A
3. RT 寄存器的内容被传送到执行功能段中的多路选择器的其中一个输入端；还会传送到下一级流水寄存器的输入端
4. 32 位立即数被传送到左移 2 位部件以便计算分支跳转地址；还被传送到多路选择器的另一个输入端
5. `lw` 指令 `AluSrc` 多路选择器控制端信号会控制多路选择器选择 32 位立即数 Signlmm进行输出并送到 ALU 的输入端 B 
6. ALU 在其控制信号 `AluOp` 的控制下进行相应运算并输出运算结果到下一级流水寄存器的输入端 AluResult
	- 对于 `lw` 指令 ALU 会将来自输入端 A 的 RS 寄存器的内容与来自输入端 B 的 32 位立即数进行相加，输出的结果是后续访存功能段所需的访存地址
	- 由于 ALU 可进行多种运算操作，取决于指令集和具体设计实现，因此 ALU 的控制信号 `AluOp` 用 X 表示而不是简单的一个比特 0、1 表示

故执行功能段的主要功能是执行 ALU 运算操作以及计算分支目标地址和相应的分支跳转控制信号 `BranchAddr`
只不过当前不是分支跳转指令 `beq`，因此没有分析分支跳转的情况
🧚‍♀️小写rt是控制寄存器堆写操作的，lw要写回到RT里面
4️⃣在下一个时钟节拍的控制下进行的相关操作：
执行功能段与访存功能段之间的流水寄存器的**各输入端的控制信号及数据**被锁存到执行功能段与访存功能段之间的流水寄存器，以便在访存功能段使用或传递给后续功能段
此时 `lw` 指令进入到了访存功能段进行处理，其后的 `sw` 指令、 `beq` 指令、`add` 指令分别进入到了执行功能段、译码功能段、取指功能段进行处理
`lw` 在访存功能段处理如下：
1.  在传递到访存功能段的控制信号中，只有写存储器控制信号在访存功能段使用，**取值为 0** 表示对数据存储器进行的是**读操作**而不是写操作
2. 目的寄存器的编号 rt被传送到下一级流水寄存器的输入端
3. RT 寄存器的内容被送入到数据存储器的待写入数据输入端；PS：For `lw` 指令送入到数据存储器的数据输入端的 RT 寄存器的内容不起作用
4. 在之前的执行功能段中的 ALU 计算结果 `AluResult` （即访存地址）被送入到数据存储器的地址输入端，还会送入到下一级流水寄存器的输入端
5. 3&4 故数据存储器中访存地址中的数据被读取并传送到下一级流水寄存器的输入端

访存功能段的主要功能：向数据存储器中的指定地址处写入数据，或从数据存储器中的指定地址处读出数据
对 `lw` 指令是从数据存储器中读出数据![[截屏2026-07-05 22.22.56.png]]
5️⃣在下一个时钟节拍的控制下进行的相关操作：
访存功能段与写回功能段之间的流水寄存器的**各输入端的控制信号及数据**被锁存到访存功能段与写回功能段之间的流水寄存器，以便在写回功能段使用或传递给后续功能段
此时 `lw` 指令进入到了写回功能段进行处理，其后的 `sw` 指令、 `beq` 指令、`add` 、`addi` 指令分别进入到了访存功能段、执行功能段、译码功能段、取指功能段进行处理
`lw` 在写回功能段处理如下：
1. 在这两个传递到写回功能段的控制信号中，**寄存器写**`RegWrite` 控制信号用于译码功能段中寄存器组的写入控制
2. `MemToReg` 控制信号用于写回功能段中的多路选择器，选择其两路输入中的哪一路进行输出
3. 目的寄存器的编号 `rt` 被传送到译码功能段中的寄存器组的写入寄存器编号输入端
4. 从访存功能段中的数据存储器中读出的数据被送回写回功能段中的多路选择器的其中一个输入端
5. 执行功能段中的 ALU 计算结果 `AluResult` 被送入写回功能段中的多路选择器的另一个输入端  
For `lw` 指令该多路选择器的控制信号会控制多路选择器**选择数据存储器的输出数据**进行输出，进而该数据被传送到译码功能段中的寄存器组的待写入数据的输入端![[截屏2026-07-05 22.34.57.png]]
🧚‍♀️看着图是可以明白但是自己模拟一遍肯定还原不出来完整流程只能有个大概
6️⃣当下一个时钟节拍到来时 `lw` 指令离开流水线，而寄存器组完成数据写入操作，即 `lw` 指令执行完毕，后面的各条指令依次进入到下一个功能段
PS 在本例中为简单起见仅对 `lw` 指令在流水线中的执行过程进行分析，实际上 `lw` 指令在流水线中的某个功能段进行处理时后续指令也在其各自所在的功能段中进行处理，即多条指令在流水线中进行并行处理
🧚‍♀️静下心来看确实能够理解数据的流动路径；当然自己分析可能就是另一个故事了（x；这里如果 add 也要放我寄存器不是冲突了吗
### 习题❌
![[截屏2026-07-05 22.36.20.png]]
A 和 B 对 
我选 D 我感觉只有时序逻辑电路呢![[截屏2026-07-05 22.38.47.png]]
🧚‍♀️前面说了呀，控制信号控制部件不属于数据通路；CPU 由数据通路和控制部件两部分组成
![[截屏2026-07-05 22.39.53.png|200]] ![[截屏2026-07-05 22.40.12.png|200]]
组合逻辑电路输出仅取决于当前的输入；时序逻辑电路由时钟信号进行控制
故 A✅
🧚‍♀️这部分很重要机组大题要是出难点就在这里
## 流水线冲突与处理
![[截屏2026-07-05 22.43.58.png]]
 ![[截屏2026-07-05 22.42.49.png]]

![[截屏2026-07-06 11.38.53.png]]
### 结构冲突与解决方案
🧚‍♀️wd在 251 页（27）；这不是操作系统的吗
![[截屏2026-07-05 22.49.02.png]] 
- 例如执行功能段要完成运算任务和计算分支目标地址的任务，除了用 ALU 完成运算任务外还需要增加一个独立的加法部件计算分支目标地址；取指功能段要完成取指令任务和计算下一个 PC 值的任务，因此需要在功能段中增加一个加法部件计算下一个 PC 值。这样在流水线各功能段并行处理的各条指令在同一时钟周期内不会争抢同一运算部件，相应功能段都有自己独立的运算部件避免了运算冲突
- 例如指令流水线的取指功能段设置有独立的指令存储器，在访存功能段设置有独立的数据存储器
- 解决取指令和访问数据结构冲突还有另一种方案，可以比喻为插入气泡，即停顿流水线一个时钟周期延缓取指令，但是会造成流水线的性能损失，往往不采用该方案。而控制冲突用的就是这种基本解决方案
![[截屏2026-07-05 22.50.38.png|200]] 
Q：译码功能段会对寄存器组进行读操作，写回功能段会对寄存器组进行写操作，这不是结构冲突吗
![[截屏2026-07-05 22.52.44.png|250]]

例如输入端口 `R1#` 和 `R2#` 用于输入要读取的 rs 寄存器和 rt 寄存器的编号；输入端口 `W#` 用于输入要写入的寄存器编号；输出端口 `R1` 和 `R2` 用于输出从给定编号的 rs 寄存器和 rt 寄存器读取到的内容；输入端口 `WD` 用于输入待写入到给定变好的寄存器的内容
![[截屏2026-07-05 22.55.55.png]]
🧚‍♀️读口写口分离，指令存储器与数据存储器分离；互斥的应该是同一个寄存器
### 控制冲突与解决方案

假设一段时间内有 5 条指令载入到了指令流水线中，指令流水线处于满载状态，当前时刻 `beq` 指令在执行功能段进行处理![[截屏2026-07-05 22.57.28.png]]
`Branch `、` AluOp `、` AluSrc ` 这三个控制信号是 `beq` 指令在执行功能段中进行处理所需的控制信号，是 `beq` 指令在译码功能段进行处理时由控制器产生的，并且在下一个时钟节拍到来时被锁存到译码功能段与执行功能段之间的流水寄存器中，以便 `beq` 指令在执行功能段进行处理时使用
同时被锁存到流水寄存器中的以便 `beq` 指令在执行功能段进行处理时使用的还有 `rs` 和 `rt` 寄存器中的数值，其中 `rs` 寄存器中的数值被直接送入到 ALU 的输入端 A，`rt` 寄存器中的数值被送到多路选择器的其中一个输入端，在多路选择器的选择控制信号的控制下，该输入端的数值被选择输出到 ALU 的输入端 B
![[截屏2026-07-05 23.01.05.png]]
假设分支成立，即 rs 寄存器中的数值与 rt 寄存器中的数值相等，经过 ALU 的运算后 ALU 的 `equal` 标志位输出为 1，与 `beq` 指令之前在译码功能段进行处理时得到并传递到执行功能段使用的控制信号 `Branch` 进行逻辑与得到分支跳转控制信号 `BranchTaken` ，该控制信号被送入取指功能段中的多路选择器的控制端，用于选择分支目标地址送入程序计数器 PC
![[截屏2026-07-06 09.44.17.png|200]]
被锁存到 IDEX流水器的还有 `beq` 指令之前在译码功能段进行处理时得到并传递到执行功能段的用于**计算分支目标地址的 32 位立即数**，该立即数被左移两位后送入加法部件的其中一个输入端
![[截屏2026-07-06 09.48.25.png|200]]

被锁存到 IDEX流水器的还有 `beq` 指令在译码功能段进行处理时得到的顺序地址 `PC+4`，他经过 IFID 流水寄存器和 IDEX 流水寄存器的逐级锁存被传递到执行功能段中加法部件的另一个输入端，这样就得到了分支目标地址，该地址被送入取指功能段中的多路选择器的其中一个输入端，在分支跳转控制信号 `BranchTaken` 的控制下，该输入端被选择输出到 PC 的输入端，当下一个时钟节拍到来时就可以通过 PC 输出分支目标地址，从指令存储器取出分支目标地址处的指令完成分支跳转![[截屏2026-07-06 09.51.12.png]]
由于 `beq` 指令在执行功能段执行了分支跳转，因此程序不在顺序执行
之前已进入指令流水线的 `add` 指令和 `addi` 指令相当于**误取进入流水线的指令**，不应该在流水线中执行，需要从流水线中清除，为了实现指令清除，将分支跳转控制信号 `BranchTaken` 作为流水线清空信号 `Flush` 直接连接到 IFID、IDEX流水寄存器各自的同步清 0 控制端![[截屏2026-07-06 09.54.19.png]]
在下一个时钟到来时 `beq` 指令进入访存功能段进行处理，而取指功能段取出分支目标地址中的新指令，并且之前锁存在 IFID、IDEX 流水寄存器中的数据和控制信号全部清 0 ![[截屏2026-07-06 09.56.00.png]]
即执行功能段和译码功能段的指令都变成了 `nop`
MIPS 32 指令集中 `nop` 指令功能是将 0 号寄存器的值左移 0 位，结果尝试写回 0 号寄存器，但该写入被硬件忽略因为 MIPS 的 0 号寄存器值恒为 0 且只读，故该指令等同于空操作指令 nop，不改变 CPU 状态，其 2 进制编码为 32 位全 0，故常 called 全零指令
综上，执行功能段和译码功能段中处理的指令变成了 `nop`，误取入流水线的 `add` 指令和 `addi` 指令被成功清除，这就是分支跳转引起的控制冲突及解决方案
Q：根据以下指令画时空图
![[截屏2026-07-06 10.00.36.png]]
A：![[截屏2026-07-06 10.46.35.png]]
第 4 个时钟到来时 `beq` 指令进入到访存功能段处理，其后的 `add` 和 `addi` 这两条指令被清空，成为空操作指令，可比喻为插入了两个气泡，而 `bne` 指令在取指功能段被取出
第 5 个时钟到来时 `beq` 指令进入到写回功能段处理，两条空 nop 指令分别进入到访存功能段和执行功能段处理，而 `bne` 指令进入到译码功能段处理，`lw` 指令在取指功能段被取出
第 6 个时钟到来时 `beq` 指令**从指令流水线中流出，也就是执行完毕**。两条空 nop 指令分别进入到写回功能段和访存功能段处理， `bne` 指令进入到执行功能段处理，`lw` 指令进入译码功能段处理，`sw` 指令在取指功能段被取出
第 6 个时钟到来时之前插入的第一个空操作指令**从指令流水线中流出，也就是执行完毕**。第二个空 nop 指令分别进入到写回功能段， `bne` 指令进入到访存功能段处理，其后 `lw` 和 `sw` 被清空成为空操作指令，第 7 条指令在取指功能段被取出
将分支指令提前到译码功能段中执行的情况如下：![[截屏2026-07-06 10.46.45.png]] ![[截屏2026-07-06 10.51.24.png]]
本节课介绍的分支指令在执行功能段执行出现控制冲突的处理方案：将分支跳转控制信号 `BranchTaken` 作为流水线清空信号 `Flush` 直接连接到这两个流水寄存器各自的同步清 0 控制端进而在下一个时钟节拍到来时将分支指令后面的两条指令清空为空操作指令 `nop`
To 将分支指令的执行提前到译码功能段，进而少插入 1 个空操作指令以减少时钟周期的浪费
在原方案上修改：在译码功能段增加比较器和地址加法器。现代 MIPS 处理器一般都在译码功能段执行分支指令并配合分支延迟槽技术，减少分支指令带来的流水线损失（超纲）![[截屏2026-07-06 11.37.42.png]]
🧚‍♀️同步清零，原先是一条一条按序执行，现在是按分支执行，(add, addi) 是一个分支；同步清零，原先是一条一条按序执行，现在是按分支执行，(add, addi) 是一个分支；继续加硬件是吧 
### 数据冲突与解决方案 
#### 数据冲突（冒险）
写后读 read after write
指令 1 进入到指令流水线的写回功能段处理时才能将结果写回到位于译码功能段中的寄存器组![[截屏2026-07-06 11.41.48.png]]
PS：蓝色加粗是在写回功能段使用的相关控制信号；黑色加粗是在写回功能段使用的相关数据信号；都是通过之前的各级流水寄存器的逐级锁存进而传递到写回功能段的
`WriteReg#` 数据信号传送的是**待写回寄存器的编号**，对指令 1 就是寄存器 R 2 的编号 2；`WriteBackData` 数据信号传送的是**待写回寄存器的内容**，对指令 1 就是寄存器 R 3 的值与寄存器 R 4的值进行逻辑与运算的结果
![[截屏2026-07-06 11.49.54.png]]
**对寄存器组进行读操作**是在指令**处于译码功能段**时进行处理的，对于指令 2 读取的是寄存器 R 2 和寄存器 R 4 的内容。
但此时指令 1 还没有流入写回功能段将运算结果写回到寄存器 R 2 中而指令 2 就已经位于译码功能段对 R 2 进行了读操作
‼️如果后一条指令的源操作数是前一条指令的目的操作数，该情况属于 RAW 相关
MIPS 标准 5 段流水线中存在 RAW 冲突![[截屏2026-07-06 11.49.42.png]]
上述情况一般不会存在数据冲突，但是在**乱序调度流水线**（超纲）中可能出现冲突![[截屏2026-07-06 11.51.32.png]]
指令 1 和 2 中的寄存器 R 4 是读后读的数据相关
讨论数据冲突读后读不被认为是一个问题
![[截屏2026-07-06 11.53.29.png]]
 WAW 指的是：两条指令都要写同一个目的寄存器，但**后面的指令先写了，导致最终结果错误。**
 ![[截屏2026-07-06 11.54.35.png]] 
#### 数据冲突（冒险）的解决方法
 ![[截屏2026-07-06 12.11.12.png]]
#### 软件方法
需要用编译器在存在 RAW 冲突的指令之间插入空操作指令
非重点
#### 硬件方法——插入气泡
插入气泡在**分支指令引起的控制冲突**中也有介绍
![[截屏2026-07-06 15.40.56.png]]
🧚‍♀️第五个取和写回在同一个 t；因为 5 在译码时 1 已经完成了；因为 I 1 在 I 5 的译码阶段之前就已经结束了写回阶段，读寄存器的值就在译码阶段；因为是 5 段流水线，I 5 用 R 3 的时候，I 1 已经存进去了，所以用的就是更改过的，所以不会冲突

🧑‍🏫复习 MIPS 处理器中的寄存器：
在 R 型指令，即所有操作数都来源于寄存器且运算结果也存回寄存器的指令中：rd 寄存器是目的寄存器，用于存放结果；rs 寄存器和 rt 寄存器都是源寄存器，分别用来存放第 1 个操作数和第 2 个操作数；上述三类寄存器都位于寄存器组中，由具体 R 型指令中的 rs、rt、rd 字段的值（寄存器编号）进行指定
本例中所有指令都属于 R 型指令

分析本例的 RAW 冲突：
指令 1 中的 rs 字段和 rt 字段的值在**译码功能段**被送入寄存器组，进而可以**从寄存器组中读出 RS 寄存器和 RT 寄存器的内容**，以便通过 IDEX流水寄存器**传递到执行功能段中进行运算**
**运算结果**会通过 EXMEM 流水寄存器**逐级传递到写回功能段**
指令 1 中 rd 字段的值（rd 寄存器的编号）会通过 IDEX、EXMEM、MEMWB流水寄存器逐级传递到写回功能段
PS：上述通过各级流水寄存器逐级传递**运算结果与 rd 寄存器编号的操作**是在**时钟节拍的控制下逐个功能段进行的**：当指令 1 进入写回功能段处理时运算结果与 rd 寄存器的编号也同时传递到了写回功能段，该 R 型指令在写回功能段将运算结果写回到位于译码功能段的寄存器组中的指定变好的 rd 寄存器
当指令 1 进入写回功能段时，后面与其存在 RAW 相关的指令 2～5 分别进入到了访存、执行、译码、取指功能段
指令 2 和指令 3 都已经过译码功能段，并在译码功能段读取了寄存器 R 3 的内容，指令 4 正处于译码功能段读取 R 3 的内容
然而此时处于写回功能段的指令 1 正在将指令 2～指令 5 都需要用到的运算结果写回到 R 3
因此指令 2～4 都属于在指令 1 将运算结果完全写入到寄存器 R 3 之前就对 R 3 进行了读取（读取到的不是实际想要的值），属于 RAW 冲突
此时指令 5 处于取指功能段，当下一个时钟到来时指令 1 从流水线流出，即指令 1 执行完毕，其运算结果已写回寄存器 R 3，指令 5 进入译码功能段对 R 3 进行读取，读取到的是所需要的运算结果，因此指令 1 和指令 5 虽然是 RAW 相关但不会产生 RAW 冲突
🧚‍♀️这里说的是 1，2，3，4，5 顺序执行 1 和 5 之间差 4 个时钟周期补了 1 存和 5 取的时间差故 5 取时 1 已经写回故不冲突
采用插入气泡的方案解决 RAW 冲突的时空图：
![[截屏2026-07-06 16.13.00.png]]
🧑‍🏫1️⃣由上图可知处于译码功能段的指令 2 和执行功能段的指令 1 存在 RAW 相关，因此需要在指令流水线的逻辑架构中**增加相应的硬件逻辑**，对处于译码功能段的指令与处于执行功能段的指令的**数据相关性进行检测和处理**，否则会产生 RAW 冲突
**这样第 4 个时钟到来时**，指令 1 进入到访存功能段处理，指令 2 被阻塞在译码功能段，指令 3 被阻塞在取指功能段，在执行功能段插入气泡，即空操作指令，**消除**了处于译码功能段的指令与处于执行功能段的指令的**数据相关性**
2️⃣然而当前处于译码功能段的指令 2 和访存功能段的指令 1 仍存在 RAW数据相关性，故仍需要在指令流水线的逻辑架构中增加相应的硬件逻辑对处于译码功能段的指令与处于访存功能段的指令的**数据相关性进行检测和处理**，否则会产生 RAW 冲突
这样第 5 个时钟到来时指令 1 进入到写回功能段处理，其后的空操作指令进入访存功能段处理，指令 2 仍被阻塞在译码功能段，相应的指令 3 仍被阻塞在取指功能段，在执行功能段插入气泡，即空操作指令，**消除**了处于译码功能段的指令与处于访存功能段的指令的**数据相关性**
3️⃣然而当前处于译码功能段的指令 2 和写回功能段的指令 1 仍存在 RAW 数据相关性，故仍需要在指令流水线的逻辑架构中增加相应的硬件逻辑对处于译码功能段的指令与处于访存功能段的指令的**数据相关性进行检测和处理**，否则会产生 RAW 冲突
这样下一个时钟到来时指令 2 仍被阻塞在译码功能段，相应的指令 3 仍被阻塞在取指功能段，指令 1 从流水线流出即执行完毕，之前插入的两个空操作指令分别进入写回功能段和访存功能段，在执行功能段插入气泡，即空操作指令，**消除**了处于译码功能段的指令与处于写回功能段的指令的**数据相关性**
实际上为了防止处于**译码**功能段的指令和处于**写回**功能段的指令产生 **RAW 冲突**，**可以在时钟信号上想办法**，使得在时钟信号的控制下先完成写回再完成读取，这样就不用插入气泡
![[截屏2026-07-06 16.31.34.png]]
进而提高指令流水线的吞吐量，因此存在 RAW 相关的相邻指令之间只需要插入连续两个气泡即可避免产生 RAW 冲突
🧚‍♀️实际上，可以修改硬件实现只插入两个，不修改就要插入三个
##### 先写回后读取
通过对 CPU 时钟周期进行功能划分To 不用插入气泡解决译码功能段和写入功能段的 RAW 冲突
接下来基于以上分析对**硬件逻辑实现分析**：
下图是基于单周期 MIPS 的专用数据通路改造的标准 5 段流水线逻辑架构并且实现结构相关和控制相关的处理（不会产生结构冲突和由分支指令引起的控制冲突），且该逻辑架构实现了本节课刚刚分析过的对 RAW 相关进行处理的硬件逻辑
![[截屏2026-07-06 16.57.48.png]]
CLK 是 CPU 的时钟信号，在下跳沿触发寄存器组的写入操作，在低电平期间读取寄存器组的寄存器内容，在上跳沿触发各流水寄存器的锁存![[截屏2026-07-06 16.35.52.png]]
假设当前处于某个时钟周期的高电平期间，指令 1 位于写回功能段处理，指令 1 有两个相关控制信号，1️⃣一个用于寄存器组的写入使能 2️⃣另一个用于选择待写入寄存器组中指定寄存器的数据，是来自执行功能段的运算结果
1️⃣写回功能段被传送回寄存器组的数据信号 `WriteReg#`，是指令 1 在**译码功能段处理时**从其指令字取出并通过译码功能段后面的各流水寄存器锁存逐级传递到写回功能段的目的寄存器的编号；2️⃣写回功能段被传送回寄存器组的数据信号 `WriteBackData` 是指令 1 在**执行功能段处理时得到的**并通过执行功能段后面的各流水寄存器锁存逐级传送到写回功能段的运算结果
当时钟下跳沿到来时指令 1 的运算结果会写入到寄存器组中给定编号的寄存器；时钟下跳沿之后的低电平期间在译码功能段进行处理的与指令 1 存在 RAW 相关的指令 4 从寄存器组中读取相应寄存器的内容
综上实现对同一个寄存器在**写回功能段**先写回再在**译码功能段**再读取的操作，进而解决译码功能段和写回功能段的RAW 冲突
当时钟**上跳沿**到来时，指令流水线中的各条指令各自进入到下一个相应的功能段，并且锁存各自所需的控制信号和数据信号到相应的流水寄存器中，以便在所进入的功能段中使用
![[截屏2026-07-06 16.57.10.png]]
🤨执行指令四才可以用时钟信号解决冲突的方法，指令 2、3 只能用插入气泡这种降低流水线吞吐量的方法
##### 相关性检测与处理
1️⃣**译码功能段中的指令与执行功能段中的指令存在 RAW 相关：** 译码功能段中的指令与访存功能段中的指令存在 RAW 相关
将 RAW 相关检测逻辑生成信号记为 RAWHazzard，写出生成该信号的逻辑表达式![[截屏2026-07-06 17.17.48.png]]
处于译码功能段的指令需要读 rs 寄存器并且 rs 寄存器编号不为 0（编号为 0 的寄存器是恒 0 寄存器，读出为 0 写入无效，因此不需要考虑 0 号寄存器的 RAW 相关）
![[截屏2026-07-06 17.20.06.png]]
处于执行功能段的指令，其相应的写寄存器控制信号为 1，代表该指令最终会对寄存器进行写操作
![[截屏2026-07-06 17.33.20.png]]
处于执行功能段的指令其最终写寄存器时使用的编号与处于译码功能段的指令读寄存器使用的编号相同，即同一个寄存器
当以上三部分全部成立，编码检测到了当前处于译码功能段的指令与处于执行功能段的指令**在 rs 寄存器的访问上存在 RAW 相关**，同理可以得出译码功能段的指令与处于执行功能段的指令**在 rt 寄存器的访问上存在 RAW 相关**的逻辑表达式如下：
![[截屏2026-07-06 17.35.37.png]]
2️⃣**译码功能段中的指令与访存功能段中的指令存在 RAW 相关：** 参考上面两个表达式
![[截屏2026-07-06 17.36.39.png]]
以上 4 行表达式其中任意一行结果为 1，就表明检测到了 RAW 相关，因此将四行进行逻辑或即可得到 RAW 相关的检测结果
![[截屏2026-07-06 17.40.37.png]]
结果为 1 暂停对当前处于取指功能段的指令与处于译码功能段的指令的处理，并插入气泡来防止 RAW 冲突的产生
插入气泡可参考解决由分支指令引起的控制冲突时使用的 1 清空信号 Flush，当检测到 RAW 相关时仅对译码功能段后面的 IDEX 流水寄存器进行情况进行即可，不需要情况译码功能段前面的流水寄存器 IFID 流水寄存器，并且还要**保证程序计数器 PC 的值不变**，这样就暂停了对处于取指功能段的指令以及处于译码功能段的指令的处理
实现这一点硬件上需要控制程序计数器 PC 和 IFID 流水寄存器的使能端，当使能端为 1 时 PC 和该流水寄存器正常工作；为 0 时忽略时钟输入，PC 和该流水寄存器的值保持不变
综上得出以下处理逻辑
![[截屏2026-07-06 17.45.47.png]]
该处理逻辑既处理由分支指令引起的控制冲突，也处理由 RAW 相关引起的 RAW 冲突：
将 RAW 相关检测结果作为暂停信号 Stall，取反后送入 PC 的使能端和 IFID流水寄存器的使能端
若出现分支跳转则清空 IFID 流水寄存器
若出现分支跳转或 RAW 相关时需要清空 IDEX 流水寄存器
右边是基于左边的逻辑表达式设计的 RAW 相关处理的组合逻辑电路（数电）
🧚‍♀️PC. EN：程序计数器 PC 的使能信号，决定是否更新 PC 的值。 IF/ID. EN：IF→ID 阶段流水线寄存器的使能信号，决定是否锁存当前的指令和 PC 值。
![[截屏2026-07-06 17.51.07.png]]
🧑‍🏫提示译码功能段和写回功能段的 RAW 冲突已采用对 CPU 时钟信号的功能划分解决了，即译码功能段和写回功能段不存在 RAW 相关；译码功能段和执行功能段的 RAW 冲突、译码功能段和访存功能段的 RAW 冲突采用插入气泡解决
🧚‍♀️硬件那块没听懂，但是这个好像就和前面那个例题差不多?；延迟两个时钟周期；因为流水线最后一步完成，数据写进去了，就不会产生冲突；这和前面感觉没区别啊，都是 ID 段阻挡，WB 段不放行；写对啦 ww  虽然分析底层硬件数据流有些困难但是抽象出来的阻塞逻辑还是不难的
🤨我也写对了
![[截屏2026-07-06 18.00.30.png]]
#### 硬件方法——数据旁路
插入气泡会显著降低 RAW 的吞吐量
![[Pasted image 20260706180245.png]]
![[截屏2026-07-06 18.04.01.png]]
🧑‍🏫由单周期 MIPS 处理器的专用数据通路改造成的流水线架构进行分析，进而得出如何通过数据旁路技术解决由 RAW 相关引起的 RAW 冲突
![[截屏2026-07-06 18.06.32.png]]
假设在某个时钟周期这条逻辑与指令和后面的减法指令分别进入到执行功能段和译码功能段进行处理，给出指令中相关寄存器的名称，其中 rs 和 rt 寄存器是两个用于存放源操作数的寄存器，由指令在译码功能段处理时进行读取；rd 寄存器是目的寄存器：用于存放指令在**执行功能段**处理时得到的**运算结果**或者在**访存功能段**处理时得到的**从数据存储器中读取到的数据**
这些指令寄存器的编号在指令处于**译码功能段**处理时被送入到了**相应器件的输入端口**
🧑‍🏫回忆插入气泡的解决方案
A：将后一条指令阻塞在译码功能段，将前一条指令仍然向前逐个功能段处理，相应前一条指令后面要插入气泡（空操作指令）
此时当前一条指令进入写回功能段时其后面插入了两个空操作指令，使得被堵塞在译码功能段的指令与位于执行功能段的空操作指令以及位于访存功能段的空操作指令都不会产生 RAW 冲突
🧚‍♀️我到现在都没搞明白 R 3 在哪；r 3 在寄存器组里，是目的寄存器
🧑‍🏫不采用插入气泡的方案
`and` 指令在执行功能段进行处理时得到的运算结果如下
![[截屏2026-07-06 20.08.22.png]]
该运算结果实际时后一条指令所需的其中一个源操作数
但当该指令 `and` 进入到写回功能段时才能将其写入到寄存器 R 3 中，然而 `sub` 指令当前在译码功能段进行处理时就已经读取了其所需的两个源操作数![[截屏2026-07-06 20.12.36.png]]
🧚‍♀️此图为了抽象省略了一些细节，但我感觉这个细节还是挺关键的？正确的图在后面电路实现有体现。若错误理解的话可能会和后面习题的弹幕一样对气泡插入时机有错误认知）；注意，这里的数据旁路箭头实际上是再加了一个选择器，用于选择数据旁路的数据和原本通过锁存器输入的数据，而非直接覆盖锁存器。
🧑‍🏫当前位于译码功能段进行处理的指令与位于访存功能段进行处理的指令存在 RAW 相关，锁存在 EXMEM 流水寄存器中的其中一个数据是最前面 `and` 指令之前在执行功能段进行处理时得到的运算结果，该运算结果实际上就是第三条指令所需的其中一个源操作数，当 `and` 指令进入写回功能段时才能将其写入到寄存器 R 3，而锁存在 IDEX 流水寄存器中的其中两个数据是中间 `sub` 指令之前在译码功能段进行处理时从寄存器组中读出的两个源操作数；`(R6)-(R5)` 是中间 `sub` 指令在执行功能段的运算结果
`or` 指令在译码功能段进行处理时从寄存器组读取到的两个源操作数，显然从寄存器 R 3 读出的源操作数实际上并不是该指令真正需要的源操作数
![[截屏2026-07-06 20.13.53.png]]
当**下一个时钟到来时**，这三条指令各自进入下一个功能段进行处理，**同时**他们各自所需的数据也被锁存到相应的流水寄存器中以便他们各自当前所在功能段使用
位于执行功能段进行处理的指令其所需的锁存在该流水寄存器中的其中一个源操作数是错误的，真正所需的正确的源操作数实际上锁存在 MEMWB 流水寄存器，故将其重定向到执行功能段的合适位置供 `or` 指令在执行功能段中进行处理时使用
![[截屏2026-07-06 20.13.28.png]]
PS：为了统一处理，该数据旁路是从**写回功能段中的多路选择器的输出端引出**的![[截屏2026-07-06 20.30.44.png]]
将访存指令 `lw` 在访存功能段进行处理时从数据存储器读出的数据通过数据旁路重定向到执行功能段的合适位置供当前执行功能段进行处理的加法 `add` 指令使用
但是这么做的缺点：使得执行功能段的关键路径延迟从原本 ALU 进行计算所需的时长增大到了 ALU 计算时长+在访存功能段读取数据存储器所需的时长
对数据存储器的访存延迟远大于对寄存器的访存延迟
举例 1 和举例 2 只涉及到寄存器的访问，举例 1 是存在 RAW 相关的两条相邻指令中前一条指令并不是访存指令；举例 2 的情况是第三条指令与第一条指令存在 RAW 相关，但第一条指令并不是访存指令
![[截屏2026-07-06 20.38.13.png]]
🧚‍♀️旁路只需要将结果线的中间结果引到 EX 中就行吧，不用再次访问寄存器吧；访存指令是指需要访问主存的指令，而 and sub or 这种是在寄存器里完成指令的，不需要访问主存；第一个指令在写回周期，而不在访存周期里 ![[截屏2026-07-06 20.48.01.png|300]]
举例：当译码功能段检测到 Load-Use 相关后，在执行功能段插入一个气泡![[截屏2026-07-06 20.52.09.png]]
访存指令在访存功能段进行处理时从数据存储器读取的数据，![[截屏2026-07-06 20.53.16.png|150]]
当下一个时钟到来时访存指令进入到写回功能段，相应的数据被锁存到该流水寄存器中，而加法 `add` 指令进入执行功能段进行处理，此时可以采用下图中的数据旁路将之前从数据存储器中读出的数据重定向到执行功能段的合适位置，以便加法指令在执行功能段进行处理时使用，这样消除了两条相邻指令的 Load-Use 相关![[截屏2026-07-06 20.54.06.png]]
🧚‍♀️为什么又只需要一个气泡了不该是两个吗；刚刚已经说了，只有访存的时候数据旁路很慢，所以再插入一个气泡，就可以在写回里面进行数据旁路处理了![[截屏2026-07-06 20.56.57.png]]
![[截屏2026-07-06 20.57.21.png]]

同理用于解决 RAW 冲突的数据旁路机制，如果将分支指令提前到译码功能段执行就需要增加**去往译码功能段的数据旁路**
![[截屏2026-07-06 20.59.07.png]]
如果将 ALU 输出的运算结果通过数据旁路之前重定向到译码功能段的合适位置供当前在译码功能段进行执行的分支指令使用，虽然可以消除 RAW 冲突但会造成译码功能段的关键路径延迟时间大大增加
因为相较于对寄存器的访问 ALU 的运算需要花费较长的时间，故不应该采用下面的数据旁路
![[截屏2026-07-06 21.02.49.png]]
应该在这两个指令之间插入一个气泡，否则会使得译码功能段的关键路径延迟时间大大增加，根据短板效应指令流水线的频率会严重降低![[截屏2026-07-06 21.06.13.png]]
随着加法指令进入到访存功能段，其在执行功能段进行处理时得到的运算结果也同时被锁存到了 EXMEM 流水寄存器中，可通过数据通路将运算结果重定向到译码功能段的合适位置，供当前在译码功能段进行执行的分支指令使用，这样消除了 RAW 冲突
![[截屏2026-07-06 21.08.33.png]]
🧚‍♀️load use 相关要强制相邻啊这里错了；lw 这句指令的意思是，将 r 6 寄存器所指地址的数据加上八再放入到 r 3 中，如果直接通过数据旁路那会增加每一个指令的时间，所以增加一个气泡；这里的相邻不一定要程序相邻，只要时间重叠就算 load-use 相关；但会造成译码功能段的关键路径延迟时间大大增加, 而应采用插入气泡的方法
![[截屏2026-07-06 21.10.40.png]]
🧚‍♀️r 4 不需要访问数据寄存器，在执行阶段就出结果了；懂了就是 ALU 和访存会增大 T 所以添加一个 nop 等下一个时钟周期到来时把锁存的数据转发过去就不用增大 T 了
![[截屏2026-07-06 21.11.20.png]]
由单周期 MIPS 专用数据通路改造来的标准 5 段指令流水线的基础上使用数据旁路解决 RAW 冲突的硬件逻辑架构
![[截屏2026-07-06 21.12.13.png]]
在执行功能段多加了两个多路选择器，1️⃣从访存的数据存储器引出一条数据旁路（绿色）连接到两个多路选择器各自的 2 号输入端口，2️⃣从写回的多路选择器的输出引入一条数据旁路（橙色）连接到两个多路选择器各自的 1号输入端口 3️⃣两个多路选择器各自的 0号输入端口分别连接的是前面 IDEX 流水寄存器锁存的 rs 寄存器的内容输出端和 rt 寄存器的内容输出端 4️⃣蓝色是用于两个多路选择器各自的选择输出控制端的信号，⚠️本例中给出的数据旁路是基于执行功能段执行指令的，并没有实现将分支指令提前到译码功能段执行时相应的数据旁路，否则需要增加去往译码功能段的数据旁路
![[截屏2026-07-06 21.17.36.png]]
LoadUse 相关还得插入气泡，检测 LoadUse 相关的逻辑表达式：处于译码功能段的指令需要读 rs 寄存器，并且 rs 寄存器的编号不为 0（读出为 0 写入无效的恒 0 寄存器）；处于执行功能段的指令其相应的存储器读控制信号有效，意味着该指令进入访存功能段进行处理时会对存储器进行读操作；处于执行功能段的指令其最终写寄存器时使用的编号与处于译码功能段的指令读寄存器使用的编号相同，当这三部分都成立表明当前处于译码功能段指令与处于执行功能段指令在 rs 寄存器的访问上存在 LoadUse 相关
PS: 图中 `MemToReg` 信号和 `MemRead` 信号同步，可以相互代替![[截屏2026-07-06 21.22.16.png]]
处理逻辑就是插入气泡![[截屏2026-07-06 21.22.34.png]]
两个多路选择器各自的控制信号在译码功能段产生并送入该流水寄存器，以 `rs` 寄存器进行说明，如果检测到译码功能段与执行功能段存在 RAW 相关则控制多路选择器将自己的 2 号输入端口的数据进行输出；如果检测到译码功能段与访存功能段存在 RAW 相关则控制多路选择器将自己的 1号输入端口的数据进行输出否则未检测到 RAW 相关则控制多路选择器将自己的 0 号输入端口的数据进行输出![[截屏2026-07-06 21.25.00.png]]
🧚‍♀️i 4 和 i 5 不是会在 alu 那里产生 loaduse 相关吗；这里 add 也不是分支跳转指令，为什么不能在进入 EX 段之后再插入气泡啊；EX 和 MEM 没有相关性就顺序执行有就加一个 nop 放在 EX 阶段卡 ID 就行
### 习题
 ![[截屏2026-07-06 11.55.20.png]]
 D 会 B 会 C 属于控制冲突
 故 A ![[截屏2026-07-06 11.58.07.png]] ![[截屏2026-07-06 11.58.15.png]] ![[截屏2026-07-06 11.58.29.png]] ![[截屏2026-07-06 11.58.58.png]]
 B
 转发机制就是数据旁路
 🧚‍♀️这里选项 D 的 add 指令是 R 2+R 6 而不是 R 5+R 6，否则也会发生数据冒险；c 选项属于写后写类型数据相关，只有在乱序调度流水线会有可能数据冒险；考研里默认 i 3 之后才执行 i 4，否则称为乱序，所以 D 错误，希望能帮助有同样疑问的同学理解；这里我以为 i 3 和 i 4 是可以任意排序执行指令流水线，还在想为啥不选 D；这是个指令序列，按顺序来的。应该要这样理解吧。 ![[截屏2026-07-06 12.05.12.png]]
 ![[截屏2026-07-06 12.06.15.png]]
 C ![[截屏2026-07-06 12.08.04.png]] ![[截屏2026-07-06 12.07.56.png]] ![[截屏2026-07-06 12.08.14.png]]
 🧚‍♀️C 选项是 WAR 读后写