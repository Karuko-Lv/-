CPU 内部控制器作用：产生控制信号序列，以控制对 CPU 内部单总线的使用；每个节拍需要的控制信号是由 CPU 内部的控制器产生的
Q：how to 产生控制信号
![[截屏2026-07-03 17.11.13.png]]
由上图知控制器有多个输入源，CPU 时钟是最基本的输入源：控制器何时产生操作控制信号、这些操作控制信号持续多久都取决于 CPU 时序
🧚‍♀️27 王道 202；cpu 内部总线在王道 220，时序的内容王道没有；时序也考啊听听吧
## 时序发生器
## 三级时序硬布线控制器
## 现代时序硬布线控制器
## 微程序控制器
## CPU 的时序及其控制方式
### CPU 的时序
#### 三级时序
##### 状态周期
##### 节拍电位
##### 节拍脉冲
![[截屏2026-07-03 17.35.15.png]]
🧑‍🏫状态周期是完成一个**基本操作**的时间单元。例如指令周期的取指周期是完成取指令这个基本操作的时间单元；指令周期的计算周期是完成**计算操作数有效地址**这个基本操作的时间单元；指令周期的执行周期是完成指令执行这个基本操作的时间单元；故三级时序中取指、计算、执行周期属于不同的状态周期
⚠️~={red}**计算周期🆚间址周期**=~

|周期|本质|如何得到有效地址|
|---|---|---|
|**计算周期**|**计算有效地址**|用 **ALU 运算** 得到 EA|
|**间址周期**|**取出有效地址**|从 **主存读取** 得到 EA|
🧚‍♀️间址周期只有间接寻址才有，这里根本没有间址周期
![[截屏2026-07-03 17.36.03.png]]
🧑‍🏫三级时序中取指、计算、执行周期都可以称之为状态周期，各个状态周期所包含的若干个操作阶段就是三级时序中的节拍电位，之前以 CPU 的时钟脉冲作为三级时序中的节拍脉冲，没有提出节拍电位的概念
👇某个三级时序系统的示意图![[截屏2026-07-03 17.45.34.png]]
🧑‍🏫每个指令周期信号都是周期性的出现高电平并维持一段时间，用于指示处于当前指令周期中的具体操作阶段；
图中可知每个状态周期包含 4 个节拍电位信号，以划分成更小的时间单元，分别用于指示处于指令周期的哪个步骤；
图中可知每个节拍电位包含 1个节拍脉冲，节拍脉冲**可能直接来自于 CPU 的时钟也可能是其分频信号**
⚠️上图是定长指令周期的三级时序系统，即**所有指令的指令周期包含相同数量的状态周期和节拍电位**
🧚‍♀️这玩意让我想起了 SDRAM 那幅突发式传输的图；是的这就是时序发生器，可用状态机等价描述，提醒一下学过数电的；三级时序，每个大时间划分成小的时间；机器周期划分时钟周期；感觉会看仿真图了（
#### 定长指令周期
![[截屏2026-07-03 17.47.50.png]] ![[截屏2026-07-03 17.48.05.png]]
🧚‍♀️一段一句一个字![[截屏2026-07-03 17.49.23.png]]
一般来说功能简单的指令比功能复杂的指令的指令周期短，如果采用定长指令周期 for 功能简单的指令而言会造成时间浪费
#### 变长指令周期
变长指令周期：不同指令执行所需机器周期或时钟周期不同 ![[截屏2026-07-03 17.51.18.png]]
🤨定长指令周期要求各个状态周期的节拍电位数量也得一样![[截屏2026-07-03 17.52.38.png]] ![[截屏2026-07-03 17.54.29.png]] ⚠️不要将定长指令周期和变长指令周期与定长指令系统和变长指令系统弄混![[截屏2026-07-03 17.55.17.png]]
🤨定长指令系统也可以拥有变长指令周期; 机器周期是状态周期吗（一般来说，可以认为是，但要看教材的术语。）
[chatgpt]两个完全不同的"长短"：
1. **指令长度**（Instruction Length）
2. **指令周期长度**（Instruction Cycle Length）（指令相关事件）
它们**没有必然联系**。
#### 传统多级时序控制方式
##### 同步控制方式
![[截屏2026-07-03 18.04.50.png]]
![[截屏2026-07-03 18.03.28.png]] 
##### 异步控制方式
功能部件需要发回应答信号给控制器
🧚‍♀️这个和之前学的单周期处理器多周期处理器有关系吗；单片机与外设通信；这不是半互锁吗？
##### 联合控制方式