## Cache 相关基本概念
🧚‍♀️ 27 wd 109 页
### Cache 的作用
![[截屏2026-06-16 18.19.21.png]]
🧑‍🏫由于 CPU 有较强的程序局部性所以可以知道有经常访问的数据，可以复制到 cache 中，大部分数据在 cache 中访问就可以提高速度
![[截屏2026-06-16 18.18.15.png|200]]
> **DDR 本质上就是一种改进版的 SDRAM。**

🧚‍♀️X 3 D = 用 3 D 堆叠技术大幅增加 L 3 Cache 容量，从而提高 Cache 命中率、减少主存访问次数，x 3 d 贵 1 k 的原因；Redis 不就是基于内存的数据库吗![[截屏2026-06-16 18.22.26.png]]
### 程序局部性
![[截屏2026-06-16 20.32.30.png]]
🧚‍♀️是不是类似于操作系统的快表；缓存思想；空间局部性就是在时间 1 访问地址 1，那么他接下来就有可能会访问地址 2，3.. 就跟那个数组一样，你访问了 0 号元素那么很有可能会访问 1，2，3 号元素，这个就叫空间局部性连续地址访问；可以把时间局限性理解为恋旧，也就是会一直访问空间局限性理解为就近用了一个东西后就会使用它附近的东西![[截屏2026-06-16 20.32.54.png]]
🧚‍♀️王道这里是说访问顺序和存档顺序一致才有空间局部性 ‼️

### Cache 系统的性能评价
#### Cache 系统
![[截屏2026-06-16 20.53.51.png]]
假设 CPU 要进行数据访问，为了简单起见假设数据访问为读操作 
1. CPU 首先在 cache 中查找待访问的数据（通过某种查找机制实现）
1️⃣找到了
2. 数据在 cache 中则 called 数据命中 
3. 将命中的数据读取到 CPU 即可
上述数据访问时间 called 命中访问时间,记为 $t_c$
包括 1️⃣cache 内查找时间 2️⃣cache 访问时间
![[截屏2026-06-16 20.58.26.png]]
2️⃣没找到
2. CPU 在 cache 中未查找到所要访问的数据 called 数据缺失
3. 将缺失的数据从主存调入到 cache 中
4. CPU 从 cache 中访问数据
上述数据访问时间 called 缺失补偿
包括 1️⃣cache 内的查找时间 2️⃣主存访问时间记为 $t_m$ 3️⃣cache 访问时间
由于 $t_m$ 较大所以缺失补偿粗略记为 $t_m$ 
🧚‍♀️这里白中英的书说的是，用主存读周期把该字从主存送到 cpu，与此同时把含有该字的块送到 Cache；在 cache 中没有命中，CPU 也不去主存，而是将数据从主存调入 cache，CPU 再在 cache 中访问；对，redis 未命中直接查数据库返回，同时再写入 redis；块存储；prefetch
#### 数据分块
![[截屏2026-06-16 21.06.04.png]]
🧚‍♀️数据块过大，载入 cache 中的数据块个数就会越少，越容易发生冲突导致替换；块大缺失损失大，需要消耗更多的时间从主存重新调入；数据块过大，，替换的时候替换出去的多，之前经常被使用的也可能会替换出去，所以破坏了时间局部性；redis 过大并行差![[截屏2026-06-16 21.09.08.png]]
🧑‍🏫数据分块后需要给主存的数据块和 cache 中的数据块分配地址
![[截屏2026-06-16 21.10.47.png]]
Cache 内的块地址长度小于主存内的块地址长度
数据块编址在地址映射中说明
#### 性能评价
1️⃣命中率 2️⃣缺失率 3️⃣cache-主存系统平均访问时间 4️⃣访问效率
![[截屏2026-06-16 21.17.55.png]]
🧑‍🏫r 值比较大，表明命中访问时间远小于主存访问时间，意味 cache 对性能的提升效果显著，cache 的引入大大减小了数据访问的延迟；r 值较小 cache 对性能的提升效果就不明显了
设计 cache 系统考虑到性能、成本、技术可行性、系统的整体平衡等因素，r 值不能太大，一般为 5 到 10
🧚‍♀️27 王道 p 111；缺失率+命中率=100%也就是=1，所以命中率如果是 h=30%（0.3），那么缺失率就是 1-h=0.7；cache 里面缺少 CPU 要访问的信息，从主存调入所需信息的时间 $t_m$；这里相当于求数学期望吧；可以看王道有这部分公式区别的解释，一个公式是同时访问 cache 和主存，另外一个公式是先 cache 后主存，也可以理解为一个访问主存时忽略 cache 时间，另外一个不忽略；和王道区别只是缺失访问时间的定义不一样，都是对的；r 值较大，我从主存拿了数据，能马上传到 cpu，cache 作用明显；r 值较小，我从主存拿了数据花的时间和从 cache 到 CPU 的时间一致，那末，cache 作用就不明显；太大说明 DRAM 的性能太差了； r 太大，说明 cache 和主存的性能差距很大，只要有一次没有命中，就会带来很大的效率降低，浪费了性能

## Cache 的读写流程
引出实现 cache 的关键技术
![[Pasted image 20260616221950.png]]
### Cache 读操作的基本流程
CPU 要读取慢速主存中的数据首先会在高速 cache 中**通过主存地址**进行查找，若数据命中则读取数据即可；若数据缺失则需要讲缺失的数据所在的数据块从主存调入到 cache 中之后才能读取数据
![[截屏2026-06-16 21.25.46.png]]

🧚‍♀️很像 redis 呀；redis 是同时送；袁春风书上给出了两种访问流程，你说的是其中的一种；袁春风出题人
![[截屏2026-06-16 21.27.07.png]]
如何快速查找取决于主存中数据块放置到 cache 中位置的地址映射，例如：全关联映射中通过相联存储器实现快速查找
另外如果主存载入数据块到 cache 但是 cache 满了应该如何处理-->替换策略![[截屏2026-06-16 21.29.21.png]]
🧑‍🏫 `addr` 是主存地址，将主存数据块载入到 cache 中![[截屏2026-06-16 21.32.51.png]] ![[截屏2026-06-16 21.32.59.png]]
🧑‍🏫主要因为数据缺失需要访问慢速主存
![[截屏2026-06-16 21.35.09.png]]
🧚‍♀️上节课不是说的，若进行数据访问时出现数据丢失的情况，要将缺失数据所在的数据块从慢速主存中载入 cache 中；可能直接读不如间接读快；问的是当数据没有命中时，为什么是让主存的数据保存到 cache 再从 cache 读取，而不是直接从主存读取；总结太好了，热数据冷数据；现代 CPU 无法绕过缓存直接访问或操作内；感觉特别像操作系统页面替换；很简单，这就是规定。跟你计算 1 加 1 一样。这就是 408 的深度。更多为什么可以自学黑书；简单来说，CPU 相当于上课需要的书，cache 相当于书包，主存是放在宿舍的书，上课前你要先从书包里找书，找不到就从宿舍拿放在书包里，然后再从书包中拿书上课
### Cache 写操作的基本流程
1️⃣CPU 收到待写入数据的主存地址，使用快速查找机制判断数据是否在 cache 中
2️⃣若数据命中则 CPU 将待写入数据写入 cache 中，新写入 cache 中的数据和原始数据是不一致的，成为脏数据
1. 写回策略：仅写入 cache 就返回![[截屏2026-06-16 21.46.04.png]]
2. 写穿策略：将脏数据写入慢速的主存后才能返回
![[截屏2026-06-16 21.46.27.png]]
🧚‍♀️27 王道 115；为什么写入数据还要判断数据是否在 cache 中；是判断主存数据块是否在 cache 中🤨我觉得是判断这个待写入数据的主存地址是不是在 cache 中；因为可以一个数据在 cache 中多次读写之后再存到主存；为什么会叫脏数据啊，按道理来说 cache 的才是新数据，旧的才是脏数据才对啊，这个取名有点怪；因为要保证缓存和主存的数据一致性；写数据可以理解为更新数据；数据不一致统称为脏数据；命中（Hit） 是指 CPU 访问的数据地址在 Cache 中找到了对应的块；但此时你是要“写数据”而不是“读数据”，所以虽然命中了，但仍要更新 Cache 中对应位置的内容。；王道书上的全写法（写穿）和回写法（写回）；写穿法 cache 和主存始终同步，不需要脏数据 (脏位，修改位）；非写分配与写穿搭配，写分配与回写搭配

3️⃣若数据缺失
1. 只写入慢速主存就返回![[截屏2026-06-16 21.50.02.png]]
🧑‍🏫非写指的是这种方式不用为写入数据分配 cache 数据块，因为仅仅将该数据写入到了慢速主存
2. 将缺失数据所在主存的数据块载入 cache 中，进行与写命中一样的写入流程
![[截屏2026-06-16 21.59.08.png]]
🧚‍♀️1. 写分配/非写分配 → 解决 “写缺失时是否加载数据到缓存”。   2. 写回法/全写法 → 解决 “主存何时被更新![[截屏2026-06-16 22.18.22.png]]
突发的小数量写入能提高性能，但是如果 cache 的脏数据要经常淘汰就还不如没有 cache
## 地址映射
数据访问首先要对数据进行快速查找——取决于所采用的地址映射方法

🧑‍🏫图中右侧是采用同步动态随机存取存储器SDRAM 的主存
在 cache-主存系统中主存被划分为若干个等长的数据块，数据块用大写字母 B 表示，下标表示数据块的序号，每个数据块包含若干个存储字，1个存储字可以是 1 字节、2 字节、4 字节、8 字节等
左侧是采用静态随机存取存储器的 SRAM 的 cache，cache 被划分成若干个等长的区域，called 行或槽
每个 cache 行都可以存储一个主存数据块的副本，且还包含一个有效位
将有效位清零就可以淘汰 cache 行中的数据块==>冲刷![[截屏2026-06-16 22.29.02.png]]
本例中主存有 mn 个数据块但是 cache 只有 n 个数据块
每个 cache 行还有一些空闲空间，这些空间用于载入主存数据块时去记录相关信息
![[截屏2026-06-16 22.30.20.png]]
PS：由于现在介绍地址映射，简单认为这些空闲空间仅用于记录主存数据块地址标记
🧚‍♀️每个数据块包含若干个字，也就是每个数据块包含若干个存储单元![[截屏2026-06-16 22.31.49.png]]
### 直接映射 （Direct Mapping）
![[截屏2026-06-16 22.34.35.png]] 

直接映射称之为模映射
由于该式中的量都是非负整数所以模运算=余运算
🧚‍♀️取模映射吗，一般会有冲突吧；有点散列查找的意思；哈希冲突？![[截屏2026-06-16 22.36.52.png]]
🧑‍🏫直接映射等效于将主存按照 cache 大小进行分区，**每个分区包含数据块的数量和 cache 行数相同** ![[截屏2026-06-16 22.40.15.png]]
🧑‍🏫主存数据块有了分区号之后没有必要对主存中的全部数据块进行统一编号，而是对每个分区中的数据块分别进行编号![[截屏2026-06-16 22.41.50.png]]
相当于将给主存编号的比特数量减少一部分，用于给分区编号了
🧚‍♀️应该是分区 m-1![[截屏2026-06-16 22.51.36.png]]
🧑‍🏫区内数据块号和其所对应的 cache 行的行号完全相等，通过区内数据块号可以精准定位出该数据块映射到 cache 中的固定行行号==>得到待访问信息的主存地址构成：其中主存分区号和区内数据块号合起来可以看成是待访问信息所在主存数据块的地址；数据块偏移指出待访问信息在主存数据块地址所指定的数据块中的具体位置
#### 数量关系对应 SDRAM 主存🆚SRAM cache
![[截屏2026-06-16 22.55.46.png]]
🧚‍♀️标记+索引+块内地址；绝对+相对；分页分段？地址加偏移；数据块内偏移是对cache的再分，比如我们想在第i个cache行写数据，不能随便找地方对吧，需要这个块内偏移量告诉我们写在这个cache行的那一部分这么理解对吗 uu们；实际上是一个三级定位；各位，一点要注意，s、r 和 w 是地址（二进制数）的位数！并不是区的数量、块的数量（十进制数）！；这里数据块大小应该是 2^w 个字，因为 cache 和 cpu 以字为数据交换单位；其实块内偏移也就是对数据块的编码，找到的东西是个字节也就乘字节了；按字节寻址，数据块内偏移位数里每一个二进制数可代表一个字节；这是按字节编址的，这些算的全是个数，不是字节或比特，所以最后要乘以八

🧑‍🏫之前介绍过每个cache 行的灰色部分用于存放主存分区号、用于写入策略的脏数据标志位、用于替换策略的淘汰计数等信息，现在不考虑写入和替换策略，这两类策略将在介绍完三种映射方式后介绍

![[截屏2026-06-18 15.00.24.png]] ![[截屏2026-06-18 15.02.37.png]]
Cache 的容量有一个有效位+主存分区号所占的 $(s-r)$ 位+一个数据块的容量 $2^w \times 8$ 位
🤨$2^r=n$
🧚‍♀️ Cache 的容量是位数吗，那为啥后边还要 2 的 w 次方；块内偏移不保存在 cache 中，cache 只保存标记；数据块内偏移是在 SDRAM 主存里的吧；乘 8 的原因是按字节编址, 每个地址里都存了一个字节；数据块的大小，不是单纯的加 w
![[截屏2026-06-18 15.08.07.png]]
直接映射方式下待访问信息的主存地址的构成👆，通过待访问信息的主存地址可以：
1️⃣在主存中精确定位该信息的位置
2️⃣在 cache 中快速查找该信息
🧚‍♀️数据块内偏移就是用来说明需要的数据在数据块中的位置，在哪个字节，比如一个数据块有 8 个字节，块内偏移 000 就是第 0 个字节，111 就是第 7 个字节，所以块内偏移数也能说明数据块内有几个字节
🧑‍🏫假设红色块是主存中某个待访问的信息，属于分区 1，其主存地址的主存分区号部分的值是 1；因为该信息位于分区 1 的数据块 1，因此主存地址的区内数据块号（或称之为 cache 行号或索引）值为 1；假设该信息在数据块 1 中的起始位置距离数据块 1 的起始地址偏移了 8 个字节，因此主存地址的数据块内偏移部分的值为 8==>通过该**信息的主存地址**可以精确描述出该信息在主存中的具体位置![[截屏2026-06-18 15.29.45.png]]
Q：如何通过主存地址在 cache 中进行快速查找
A：🧑‍🏫通过信息的区内数据块号即 cache 行号的取值，可以索引到该信息所在主存数据块所对应的 cache 行；直接映射规则可知主存中会有多个数据块对应 cache 中的某个固定行，例如本例中主存分区 0 的数据块 1 对应 cache 行 1、主存分区 1 的数据块 1 也对应 cache 行 1... 主存分区 m-1 的数据块 1 也对应 cache 行 1，❓如何判断 cache 行 1 中当前保存的主存数据块的副本是否是待访问信息所在的那个主存数据块呢❓❓❓
Cache 行中包含有主存分区号的作用：通过比较 cache 行中主存分区号的取值与待访问信息的主存地址中的主存分区号部分的取值是否相等，从而判断出 cache 行中的数据块副本保存的是否是待访问信息所在的主存数据块，如果相等则数据命中；不等则数据缺失。若该 cache 行中的有效位为 1 则表示该 cache 行信息有效，通过待访问信息的主存地址中的数据块内偏移部分的取值就可以在该 cache 行中的数据块副本中找到待访问信息进而从快速的 cache 中读取该信息而不用再慢速主存中读取信息；cache 未命中即数据缺失或该 cache 行有效位为 0 则需要根据待访问信息的主存地址**将主存中该信息所在的数据块载入相应的 cache 行**，相应将该 cache 行的有效位置 1 以及将该 cache 行的主存分区号设置为该信息的主存地址中的主存分区号，同时将信息送入 CPU

🧚‍♀️一个数据块有很多字节，块内偏移就是指定一个块内的具体的某个字节的地址；因为每个块内还包含多条数据信息，块内偏移可以简单认为在块内再给每条信息编的地址；就是块内地址，他不会记录到 cache 中只是通过硬件，在找到对应的在 cache 的哪一块后，对比数据是否一致；有效位是看你主存的东西有没有放到 cache 中去，cache 中某一行已经存放了主存中的某一行数据，那么有效位就是 1；*实际应该先检查有效位，然后检查分区号，不会出现缓冲命中但是不有效，不有效就是没命中。；一个数据块副本只能存一个数据块吗，那为什么会有块内偏移；因为一个块内可能有多个数据呀*；我的理解是数据块内偏移就是所在存储单元的位置，一个数据块有若干存储单元，这个 w 是标记对应单元位置的

‼️ ‼️‼️多多复习![[截屏2026-06-18 15.29.58.png]]
#### 硬件实现
🧑‍🏫下面是一个包含有 4 个 cache 行的直接映射方式的硬件逻辑实现：![[截屏2026-06-18 17.45.23.png]]
1️⃣各 cache 行的有效位用英文 valid 表示
2️⃣各 cache 行的标记字段用英文 tag 表示
3️⃣各 cache 行用于保存数据块副本的缓冲区是 $W_0 \sim W_3$（假设个数据块由 4 个存储字构成）
4️⃣1个存储字可由 1 字节、2 字节、4 字节或 8 字节构成，描述方便则以存储字为单位而不用字节或比特单位
5️⃣主存地址中的数据块内偏移部分以存储字为单位，将数据块内偏移单位用 offset 表示
6️⃣主存地址的区内数据块号就是 cache 行号/cache 行索引，英文 index 表示，其值作为行索引译码器的输入端，经过译码器输出相应的 cache 行选通信号，该信号还连接到所选中 cache 行相关的两个三态门各自的控制端，被选中 cache 行相关的位于这一列的三态门被控制打开输出，因此该 cache 行中的有效位和标记 tag 会通过三态门输出到比较器，被选通 cache 行相关的位于这一列的三态门被控制打开输出，该 cache 行中数据块副本缓冲区中的 4 个字通过三态门输出到字选择器的输入端。若比较器判断被选通 cache 行的有效期为 1 并且该行中的标记 tag 的值与主存地址中标记 tag 值相同，则比较器输出命中信号，该信号同时也会“使能“字选择器，字选择器根据主存地址中的偏移 offset 的值选择自身 4 个输入端中相应的那个输入端进行输出，即输出所选中 cache 行中数据块副本缓冲区中相应的那个字进行输出

PS：在直接映射方式中 tag 就是主存地址中的主存分区号部分，之前并没有使用 tag 这个英文单词而是使用主存分区号这个名称，与后面两种地址映射方式中描述主存地址构成统一

🧚‍♀️就是一样的，标记又不包含有效位啥的，不就相当于分区号；王道的标记位就是这儿的主存分区号即 tag；由大到小，先是在主存的哪个分区；然后说在这个分区的哪个位置，也就是映射到 cache 块的哪一行（这一步直接取，不需要再取模，硬件来做）；最后的块内偏移说在 cache 行的具体位置；先确定行，再比分区号啊；为什么不能，之前学过低位交叉编址啊，这种肯定要根据具体需要来的
![[截屏2026-06-21 13.32.58.png]]
🧑‍🏫通过几次访问过程的举例加深对直接映射的硬件逻辑实现的理解，总结直接映射方式的特点
1️⃣假设 cache 初始状态为空，则各 cache 行的有效位valid 为 0
2️⃣假设待访问信息位于主存分区 0 中数据块号为 3 的数据块中，则该信息在数据块中的偏移为 3（将十进制转换成 2 进制方便用于硬件逻辑）
3️⃣假设主存分区号由 4 位二进制数构成，因此主存空间可划分为 $2^4=16$ 个分区，从分区 0 到分区 15
PS：由于 ppt 大小该图中没有主存
4️⃣区内数据块号/cache 行号或 index 由 2 位二进制数构成，因此 cache 中包含有 $2^2=4$ 个 cache 行，从 $cache 行 0\sim cache 行 3$，与图中硬件逻辑吻合
5️⃣数据块内偏移 offset 由 2 位二进制数构成，每个 cache 行的数据块副本缓冲区包含有 $2^2=4$ 个存储字，从存储字 $w_0\sim w_3$ 和图中所示的硬件逻辑符合
🧚‍♀️这里的英文太容易让人误解了，我还以为那个 tag 是标志位我说怎么不对，原来这里的 tag 代表的是主存分区号 dd

🧑‍🏫将主存地址二进制形式代入硬件逻辑中，且用 16 进制形式记录主存地址，通过记录几次访问过程的结果，总结出**直接映射方式的特点**

第 1 个待访问信息的主存地址 $0FH$，该主存地址的 index 部分（区内数据块号）取值为 11，经过行索引译码器译码之后输出 cache 行选通信号，高电平 1为有效信号，且 cache 行选通信号还会传送到各三态门的控制端，控制端也是高电平有效；cache 行号为 3 的 cache 行被选中，由于该 cache 行中相应的三态门控制端为有效信息，因此该三态门打开输出，该 cache 行中有效位的值和标记 tag 的值通过该三态门到达比较器（🧚‍♀️为啥有效位的值为 0 啊？因为刚开始 cache 肯定是空的）；由于此时有效位的值为 0 表示 cache 行中的信息无效因此不用关心标记 tag 的值，比较器将有效位的值与 1 进行比较不相同则输出无效信号 0，表明数据未命中，该无效信号会送入字选择器使能输出 OE 的输入端，尽管被选中 cache 行中的数据块副本缓冲区相关的三态门的控制端为有效信号，该缓冲区的 4 个存储字会通过三态门输出到字选择器的相应输入端，并且主存地址的 offset 部分取值 11 会送到字选择器的选择输出控制端，然而由于自此字选择器输出使能 OE 输入的是无效信号，因此字选择器并不会讲所选择的存储字 $W_3$ 进行输出，因此第一次访问时数据缺失，即**未命中**![[截屏2026-06-21 14.51.02.png]]
🧚‍♀️王道硬件这块好像都没讲；408 本来就不考硬件；这是数电的标识符，字选择器这种常用器件都有专属引脚名称，oe 当使能就好了；别争了，这个只是老师拿来举例子让我们好理解，这个例子本身是不考的，你理解直接映射是啥就行


**未命中**需要进行**数据块载入**，将主存中待访问信息所在的数据块载入到 **cache 中被选通的 cache 行**中数据块副本缓冲区中，将待访问信息的主存地址中标记 tag 的值复制到被选通的 cache 行中的 tag 中，还要将被选通的 cache 行中的有效位置 1，表示该 cache 行中的信息是有效的
![[截屏2026-06-21 15.17.55.png]]

最后将 cache 行中数据块副本缓冲区的存储字 $W_3$ 送入 CPU 即可 

🧑‍🏫$2 CH$ 缺失因为tag 不相等，要去主存中取，替换之后还要将 cache 行中的标记 tag 进行相应的更新
‼️通过这个例子可以看出采用直接映射方式在 cache 还有空行的情况下就可能会出现替换 cache 行中原有数据块的情况，或者说是出现了冲突，因此 cache 利用率不高
![[截屏2026-06-21 15.49.28.png]]
🧚‍♀️对应的 index 是 00 和 01，也就是 L 0 和 L 1，它们的 valid 是 0，所以要载入（和前面的 0 FH 步骤一致）；虽然行号对，但分区号不对

**总结**🤨‼️‼️‼️‼️
1. `tag` 主存分区号是决定在主存 SDRAM哪个区域的，共 0 到 $m-1$  m 个区域
2. 区内数据块号是决定在 cache 高速寄存器SRAM 哪一行，共 0 到 $2^{index}$ 行
3. Offset 决定的是 cache 每一行中的数据块副本缓冲区的哪一块
![[截屏2026-06-21 15.50.46.png]]
🧚‍♀️二级存储器；脏数据在这里指就是 cache 的值之前被修改了，但是没有写入主存中，会导致和主存和 cache 数据不一致
 #### 习题课
![[Pasted image 20260621155704.png]]
![[截屏2026-06-21 15.54.00.png]]
1. 一个主存数据块有 16 个块内主存数据，所以块内地址为 4 位
2. 主存地址一共 32 位，去掉 4 位块内地址，主存块号为 $s=28$ 位
3. Cache 数据区大小为 $2^{16}$ B
4. Sram 的数据块副本是一个数据块的容量 $2^w \times 8$ 位
5. 数据块容量为什么是 $2^w \times 8$ 呢❓看硬件图理解
6. Cache 的容量有一个有效位+主存分区号所占的 $(s-r)$ 位+一个数据块的容量 $2^w \times 8$ 位
7.  Cache 的数据块副本是 16 B，即 7 位
8.  Cache 数据区大小为 64 KB，主存数据块大小为 16 B，则一共有 $2^{12}$ 行，所以 $r=12$
9. $s-r=16$ ，总容量为 $145 \times 2^{12}$
![[截屏2026-06-21 16.42.14.png]]

🧑‍🏫
1️⃣将主存划分成若干个等长的数据块，题目给出每个主存数据块大小为 16 字节
![[截屏2026-06-21 16.56.36.png|200]]
2️⃣包含若干个 cache 行的 cache，题目给定 cache 数据区的大小为 64 K 字节，而各 cache 行中数据块副本的大小与主存数据块大小相同，因此可以计算出本题中 cache 所包含的 cache 行数量![[截屏2026-06-21 16.58.33.png]]
🧚‍♀️数据块和数据块副本大小相同，所有分区并不是同时映射在数据块副本上，所以用 tag 来确认是哪个分区
3️⃣题目给出主存地址为 32 位，而cache 行的数量为 $2^{12}$ 个，因此主存地址中的 cache 行号部分所占位数为 12，因为 12 个二进制位可以表示  $2^{12}$ 个组合，每个组合作为一个 cache 行号
4️⃣主存地址中 cache 行号部分也称为 cache 行索引或 index，为了更容易理解一开始称其为主存分区中的数据块编号，简称为区内数据块号，理解直接映射规则和编址方法后将主存地址这一部分直接称之为 cache 行号即可；主存地址的块内地址部分可以称其为数据块内偏移或 offset，这里叫做块内地址（一定要**熟悉主存地址各部分的不同名称**），因为不同教材习题中使用的名称可能不同，题目给定每个主存数据块大小为 16 B 主存按照字节编址，因此可以计算出主存地址中块内地址部分所占位数为 4，因为 4 个二进制位有 16 个组合，用来对每个主存数据块中的 16 个字节进行编号![[截屏2026-06-21 17.08.52.png]]
主存分区号也称之为标记 `tag`
主存被划分成了 $2^{16}$ 个分区 
![[截屏2026-06-21 17.10.53.png|200]]
🧚‍♀️标记 (tag) 索引 (index) 偏移 (offset) 
5️⃣‼️在直接映射方式中**每个分区所包含的数据块数量与 cache 中 cache 行数量相同**，这也是主存地址中 cache 行号部分又被称为区内数据块号的原因，因此本例每个分区中包含有 0 到 4095 号共 4096 个数据块，与 cache 中的 $2^{12}$ 个即 4096 个 cache 行一一对应![[截屏2026-06-21 17.14.33.png]]
### 全相联映射（Full Associative Mapping）
规则为每个主存数据块都可以映射到 cache 中的任何一个 cache 行
==>新的主存数据块可以载入到 cache 中的任何一空行；只有当 cache 满的时候才会进行数据块替换
Q：如何描述主存中某个信息的位置呢？例如下面的红色块
![[截屏2026-06-21 17.24.48.png]]
A：只用知道该信息所在主存数据块的首地址（即主存数据块地址）以及信息在该数据块内的偏移量，便可以精确定位主存中的该信息）
- 全相连映射方式下的待访问信息的主存地址构成：由主存数据块地址和数据块内偏移两部分构成
- 与直接映射下的主存地址的三级结构不同🧚‍♀️没了区号了哈哈
 #### 数量关系对应
假设按字节编址：
![[截屏2026-06-21 17.28.59.png]]
在不考虑用于写入策略的脏数据标志位、用于替换策略的淘汰技术等信息的情况下每个 cache 行中的灰色空间仅用于存放主存数据块地址，与直接映射不同，直接映射这部分空间用于存放主存分区号，而全相联映射方式下没有主存分区这个概念
故计算出 cache 容量如下
![[截屏2026-06-21 17.42.06.png]]
其中 1 个有效位+主存数据块地址 s 位+1 个数据块的容量 $2^w \times 8$ 位是一个 cache 行的容量

🧑‍🏫通过待访问信息的主存地址不但可以在主存中精确定位该信息的位置，还可以在 cache 中快速查找该信息
Q：❓如何根据信息的主存地址在 cache 中进行快速查找呢
A：由于 cache 一共有 n 行，因此可以采用 **n 路并发比较**，即将待访问信息的主存地址中的主存数据块地址部分的值同时与各 cache 行中的主存数据块地址的值进行比较；并且将各 cache 行中有效位的值同时与 1 进行比较==>都匹配则数据命中
![[截屏2026-06-21 20.53.47.png]]
1️⃣读操作：输出相应 cache 行数据块副本中的块内偏移处的信息
2️⃣写操作：写入数据，同时将脏数据标志为 1
🧚‍♀️为什么要将脏数据标志位置为 1，如果采用写穿策略，好像没有脏数据位吧？；408 的可以跳了，不考，空降 15：28；脏数据标志位是标识该行中的数据是否已被 CPU 修改且未同步回主存，1 表示 cache 改变而主存未改变，也就是未同步，0 表示未修改或已同步
![[Pasted image 20260621221008.png]]
#### 硬件实现

图中 cache 行的有效位为 valid，其各自作为并发比较器的输入同时与 1 进行比较；各 cache 行的标记字段为 tag；
PS：在全相联映射当中 tag 对应的是主存地址中的**主存数据块地址部分**；在直接映射当中 tag 对应的是**主存地址中的主存分区号部分**
全相联映射当中没有主存分区号的概念，各 cache 行的 tag 各自作为并发比较器的输入同时与主存地址中的主存数据块地址部分进行比较，$L_0 \sim L_3$ 是并发比较器根据比较结果输出的相应的 cache 行选通信号，用于选通某个 cache 行
PS：同一时刻只有一路信号可能为有效信号，即只可能选通一个 cache 行
各 cache 行的选通信号还用于控制各 cache 行的三态门，当某个 cache 行被选通则该 cache 行的三态门被控制打开，![[截屏2026-06-21 21.20.27.png|200]] 
因此该 cache 行中的数据块副本缓冲区中的内容通过三态门传送到字选择器的输入端
为简单起见假设本例中数据块副本缓冲区可以保存 4 个存储字，记为 $w_0 \sim w_3$，一个存储字可由 1字节、2 B、4 B...构成，方便描述以存储字为单位，而不用字节或比特单位
并发比较器根据比较结果不但输出 cache 选通信号还会输出命中信号，命中信号除了向外提供通知外还会对内部字选择器的输出使能 OE 进行控制![[截屏2026-06-21 21.25.26.png]]
主存地址中的块内偏移部分用于控制字选择器将自己的哪个数据输入端的信号进行输出
本例中字选择器有 4 个数据输入端分别用于输入被选通 cache 行中的数据块副本缓冲区中的 4 个存储字，因此主存地址中的块内偏移部分占 2 位
1️⃣由于有效位是 0 所以未命中
![[截屏2026-06-21 21.29.32.png]]
2️⃣需要载入该主存地址 `OFH` 所在的主存数据块到 cache 中。AT 全相联映射方式，可将主存数据块载入到 cache 中的**任何一个空行**，假设该主存数据块载入到 cache 行 0 中的数据块副本缓冲区，则该 cache 行中标记 tag 的值需要更新位主存地址中数据块地址部分的值，且将该 cache 行中的有效位置 1 ，表示该 cache 行中的信息有效
![[截屏2026-06-21 21.32.52.png|200]]
🧚‍♀️其实只要搞懂缺失载入逻辑就行了，再对应映射规则就能懂，忽略掉低电平高电平什么的；这个世界就是个巨大的二进制

![[截屏2026-06-21 21.35.59.png]]
🧑‍🏫上述命中因为红色部分 cache 行有效位值为 1，并且该 cache 行中标记 tag 值与主存地址中数据块地址的值相同，并发比较器输出比较结果 1，高电平表示命中，该信号传送到字选择器的输出使能 OE 端，字选择器被使能输出
![[截屏2026-06-21 21.39.41.png]]

🧚‍♀️太清晰了这里完全听懂了，同时也理解了 CPU 和 Cache 传输的数据是字
![[截屏2026-06-21 21.43.26.png]]

![[截屏2026-06-21 21.45.51.png]]
🧚‍♀️直接映射根据主存地址中的 Cache 行号寻找，无需相联存储器；比较 cache 中的主存块地址，也就是比较 cache 中的内容 
![[截屏2026-06-21 21.47.19.png]]
🧚‍♀️满行且有冲突的会替换哪一行呢；详见后面会讲的替换算法部分
#### 习题
![[截屏2026-06-21 21.48.11.png]]
🤨
1. 数据块地址的位数 s 位可以
	1. 根据主存地址的大小-块内偏移长度算出来
	2. 主存空间大小/数据块大小
2. 主存数据块大小直接决定主存地址块内偏移大小 w（无论直接 or 全相联）
![[截屏2026-06-21 21.54.30.png]]
![[截屏2026-06-21 22.09.18.png]]
脏位去直接映射习题课听

![[Pasted image 20260621220912.png]]
🧚‍♀️cache 数据区的大小在这里就是无用条件了
## 组相联映射
![[Pasted image 20260621230054.png]]
🧑‍🏫目的是提高命中率同时降低查找成本
🧚‍♀️哪来取长补短，只不过是两者间取了个平衡的方式


![[截屏2026-06-21 22.11.11.png]]
🧚‍♀️果然计算机的本质就是折中和套娃；就是把 cache 分组了，然后主存中每一个块根据模运算对应不同的分组，在分组内随便映射; 1 路组相联就是直接映射，n 路组相联就是全相联映射; 算数据块号对应 cache 组号时其实就是直接映射，同一 cache 组内进行并行比对时其实就是全相联映射; 简单点就是主存数据块对应一砣 cache, 且可以随便放入里面一行
🤨组间直接映射，组内全相联映射

🧑‍🏫
1️⃣将cache 中所有 cache 行每两行划分为 1 个 cache 组，划分出 n 个组，组编号从 0 到 n-1![[截屏2026-06-21 22.17.46.png]]
2️⃣将主存划分为 $mn$ 个等长数据块，数据块的编号从 $0 \sim mn-1$
![[截屏2026-06-21 22.21.24.png]]
3️⃣主存数据块在所对应的组内采用全相联映射可对应到组内任何一个 cache 行

![[截屏2026-06-21 22.20.59.png]]
![[截屏2026-06-21 22.22.54.png]]
🧚‍♀️纯直接映射是以 cache 行数量为一个周期 (一个主存分区), 组相联是以 cache 组数量为一个周期 (一个主存分区)
🧑‍🏫当主存数据块有分区号之后没有必要对主存中全部数据块进行统一编号了，而是对每个分区中数据块进行编号，让各分区中的各个数据块编号与 cache 中各 cache  的编号一一对应相等
会让原来用于主存中全部数据块统一编号的比特数量减少了一部分，这部分用于给分区编号
🧚‍♀️王道上说的高位就是这个分区好吧
Q❓主存中某个信息的位置怎么描述![[截屏2026-06-21 22.30.56.png]]
🧚‍♀️区号、块号、偏移；TAG+分区编号+块内偏移；跟直接映射的地址一样
🧑‍🏫由于区内块号和其对应的 cache 组的组号完全相同，通过区内块号可以精准定位出该数据块所映射到 cache 中固定组的组号
区内块号=cache 组号=cache 组索引
主存分区号+区内块号合起来可看作为待访问信息所在主存数据块的地址；块内偏移指出待访问信息在主存数据块地址所指定的数据块中的具体位置
![[截屏2026-06-21 22.38.10.png]]
🧚‍♀️习惯哪个就看哪个吧，我第一次学的时候就是现在老师讲的，王道的没去管，题目应该都能做的（等下去写题，如果做不了我回来告诉你）；多对比一下然后理解，不要理解名词，而是要理解每个部分的作用；说的不一样是因为 408 说法没统一，但全要懂；**它相较于直接映射更好的是在组满之前不会出现空行和冲突**；所以其实直接映射里的 cache 行号和组相连映射里的 cache 组号实际上都是隐含的信息
#### 数量关系对应
![[截屏2026-06-21 22.37.49.png]]
2 可以换成 k，且计算总容量时忽略了写入策略的脏数据标志位，用于替换策略的淘汰计数等信息
通过待访问信息的主存地址可以在主存中精确定位该信息的位置
🧚‍♀️与直接映射不同的是乘上了组内行数
![[截屏2026-06-21 22.39.21.png]]
Q：如何快速查找
🧚‍♀️区内块号还是感觉像王道书上叫组号好理解；区内块号相同的主存块，映射到同一个 cache 组；先定位到组，组内再并发比较，这样就不会有全相连查找全部的弊端了
🧑‍🏫将待访问信息的主存地址中的主存数据分区号部分的值同时与所选定 cache 组内的各 cache 行中的标记 tag 值比较；并且将各 cache 行中的有效位的值同时与 1 进行比较，上述两个比较都匹配则数据命中
命中后根据该信息的主存地址中块内偏移部分的值在所命中的 cache 行中的数据块副本读取该信息 ![[截屏2026-06-21 22.44.02.png]]
![[截屏2026-06-21 22.44.37.png]] 

![[截屏2026-06-21 22.46.01.png]]
#### 硬件实现
![[截屏2026-06-21 22.46.28.png]]
直接映射和全相联映射的硬件逻辑实现的结合
🧑‍🏫通过访问序列了解该硬件逻辑实现进行快速查找的过程
1️⃣2 路比较结果一开始是未命中，字选择器被禁止输出
![[截屏2026-06-21 22.48.59.png]]
2️⃣2 路并发比较结果 k_0 k_1 与组索引译码器输出 2 个组译码信号 G_0 G_1 通过行译码信号逻辑分别进行逻辑与得到 $2 \times 2$ 个 cache 行选通信号 $L_0 \sim L_3$
![[截屏2026-06-21 22.52.25.png]]
由于是无效信号各三态门被控制关闭
🧚‍♀️G 是组的意思，G 0 是第 0 组，G 1 是第一组，L 1 是第一组第二个，所以是 G 0 K 1
![[截屏2026-06-21 22.55.17.png]]
![[截屏2026-06-21 22.56.57.png]]
![[截屏2026-06-21 22.58.08.png]]

🧚‍♀️注意：这个 G_r 和图中的组号 G_0、G_1 不是一个东西！不要看错了，Gr 和 k 是一个东西

## Chatgpt


```text
脏位（Dirty Bit）：
用于表示Cache行中的数据是否与主存对应数据一致。
```

具体含义：

#### Dirty = 1

表示：

```text
CPU修改了Cache中的数据
↓
主存中的对应数据还没修改
```

即：

```text
Cache ≠ 主存
```

数据是“脏”的。

例如：

```text
主存：100

↓

装入Cache

↓

CPU改成200

↓

Cache：200
主存：100
```

此时：

```text
Dirty = 1
```

---

#### Dirty = 0

表示两种情况：

##### 情况 1：从未修改

```text
主存：100

↓

装入Cache

↓

CPU没改
```

此时：

```text
Cache：100
主存：100
Dirty=0
```

---

##### 情况 2：修改后已经写回

```text
CPU改成200

↓

写回主存

↓

主存也变成200
```

此时：

```text
Cache：200
主存：200
Dirty=0
```

---

### 为什么需要脏位？

主要针对：

```text
写回法（Write Back）
```

因为写回法是：

```text
CPU改Cache
↓
先不改主存
```

等 Cache 行被替换时再决定：

```text
要不要写回主存
```

判断依据就是：

```text
Dirty Bit
```

---

替换时：

#### Dirty = 0

说明：

```text
主存已经是最新的
```

直接丢弃：

```text
Cache行
```

即可。

---

#### Dirty = 1

说明：

```text
主存还是旧数据
```

必须：

```text
Cache
↓
主存
```

先写回，再替换。

---

你可以记一句最经典的话：

> **脏位=1 表示 Cache 中的数据比主存更新；脏位=0 表示 Cache 与主存一致（或未被修改）。**

这句话在选择题和填空题里几乎不会错。
## 三种地址映射方式比较
### 关联度
**~={red}关联度 = 一个主存块允许放入 Cache 的位置数量=~**
1. 关联度=1 即1路组相联
2. 2路组相联：关联度 = 2
3. 4路组相联：关联度 = 4
4. 很少看到 3 路、5 路、6 路，原因和**硬件实现效率**有关。1024 ÷ 3 = 341.33，还要 log 2  
![[截屏2026-06-22 17.31.04.png]]
🧚‍♀️直接 1 对 1 /全相联 1 对全部/组相联 1 对 1 组；全相联＝随便放；一个固定组内的任意一行
![[截屏2026-06-22 17.31.38.png]] ![[截屏2026-06-22 17.31.55.png]] ![[截屏2026-06-22 17.32.37.png]] ![[截屏2026-06-22 17.33.47.png]]
### 与其他因素的关系
![[截屏2026-06-22 17.34.26.png]] 🤨关联度越高命中率越高; 命中时间越久（因为需要用到 k 路并发比较）；cache 行中标记 tag 所占空间越短
❌命中率高命中时间就会越少
![[截屏2026-06-22 17.42.46.png]]
🧚‍♀️直接映射中，主存地址中需要相当数量的空间存放 cache 行号，相应的 tag 也变少了；其实本质上就是将内存地址进行划分，分为三部分，只不过不同方式划分不一样![[截屏2026-06-22 19.54.40.png]] ![[截屏2026-06-22 19.55.11.png]] ![[截屏2026-06-22 19.55.21.png]]
🧚‍♀️r 是组数以 2 为底取对数，直接相联中 g 是 cache 总行数以 2 为底取对数![[截屏2026-06-22 19.56.21.png]]
这里的**“额外开销”**指的是：

> Cache 除了存储真正的数据块以外，还必须额外存储的管理信息所占用的空间和硬件成本。
> 🤨即 valid tag 这些，用来判断 CPU 访问的主存地址对应的数据是否是 cache 中存储的数据

“额外开销”主要包括：

|映射方式|Tag 空间开销|比较器开销|
|---|---|---|
|直接映射|最小|最小|
|组相联|中等|中等|
|全相联|最大|最大|

> **关联度越高，命中率越高，但实现成本（额外开销）也越大。**

🤨上图中的蓝色部分是额外开销
🧑‍🏫尽管直接映射和组相联映射都包含主存分区部分，但是划分依据不一样
### 习题
![[截屏2026-06-22 20.21.01.png]]
1. 块内偏移 $w$ = 数据块大小 $2^wB$ 
2. 关联度=1 ==> 区内数据块号 = $log _2 n$ , $n$ 是 cache 行数
![[Pasted image 20260622203233.png]]
🧑‍🏫由主存分区号所占位数得到主存被划分为 $2^{16}$ 个分区，分区编号从 0 到 $2^{16}-1$
![[截屏2026-06-22 20.33.15.png]]