## 概述
![[截屏2026-06-24 21.44.15.png]]
🧑‍🏫第一章介绍了机器指令![[截屏2026-06-24 21.45.29.png]]
🧑‍🏫⚠️第四条信息一般情况下不需要在指 令中显式给出，**下一条指令的地址是隐含在程序计数器 PC 中**，1️⃣指令顺序执行 **PC 自动加上指令长度**得到下一个指令的地址 2️⃣转移指令不按照顺序执行时需要由指令给出转移到的目的地址
### 不同层级的指令
计算机系统不同层次的用户会使用不同级别的指令![[截屏2026-06-24 22.00.57.png]]
**软件层面的指令需要编译、汇编或解释成机器指令后才能被计算机硬件执行**
一般来说一条高级语言指令会被编译或解释为多条机器指令；汇编指令通常会被汇编成一条机器指令
一条机器指令所要实现的功能往往依赖于多条微指令的执行
🧑‍🏫机器指令是计算机软件和硬件的接口，也是用户操作和使用计算机硬件的借口，故从机器指令级介绍指令系统，下一章从微指令集介绍中央处理器 
![[截屏2026-06-24 22.04.12.png]]
🧑‍🏫这里的系统软件例如操作系统层的操作系统软件；硬件逻辑电路例如属于逻辑电路层的硬件逻辑电路

## 指令的一般格式
指令一般由**操作码字段和地址码字段两部分**构成![[截屏2026-06-24 22.08.22.png]]
**地址码指明相应的操作数**，可以包含多个操作数
如何获取操作数由寻址方式决定，即寻址方式决定操作数的存放位置和访问方式，寻址方式可以显式包含在地址码字段中 or 隐式包含在操作码字段中
🧚‍♀️operation address
### 指令字长
构成指令的二进制位数 called 指令字长
![[截屏2026-06-24 22.12.56.png]]
🧑‍🏫某些复杂操作需要较长指令字长才能描述，简单操作较短指令字长即可，为了取指和译码的方便将每条指令设计出相同的长度，简单操作指令长度的冗余较多；由于指令长度固定很难扩展指令功能
🧚‍♀️梦回文件逻辑结构；各有优缺点，这不取个折中方案→_→；整一个分组定长指令系统的那位，完了，给你学到 OS 和计组的精髓了（）；ICS 里面学的是 x 86 指令集![[截屏2026-06-24 22.13.43.png]]
🧑‍🏫机器字长是计算机系统的重要性能指标之一；~={red}
 $机器字长=ALU位数=通用寄存器的宽度$
是 CPU 一次能处理的二进制数据的位数=~
