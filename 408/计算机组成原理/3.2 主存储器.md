## 静态随机存取存储器 SRAM
![[截屏2026-06-12 22.18.25.png]]

### 存储元
https://www.bilibili.com/video/BV1qG41197E4?spm_id_from=333.788.player.switch&vd_source=cd4b54541e4278a2d09f1d862fb87d76&p=50
🧑‍🏫该图是典型的由多个 MOS 管构成的 SRAM 存储元
模拟电路中 MOS 管的应用一般侧重于利用其放大和阻抗变换的特性，需要考虑其在不同工作区的行为
数字电路中 MOS 管主要用作开关，其快速响应和低导通电阻的特性使其非常适合高速数字逻辑电路设计 MOS 管的部分看成开关
由于本课程是电子数字计算机的组成和工作原理底层有数字逻辑电路构成， MOS 管的部分看成开关![[截屏2026-06-12 22.24.54.png]]
🧑‍🏫将 MOS 管看成开关利用物理电路知识分析 SRAM 存储元的工作原理，看它如何存储一位二进制信息，即比特 1 和比特 0
行选通线 X 和列选通线 Y 可看成上节课介绍的行地址译码器和列地址译码器各自的一条译码输出线，无效信号就是 0，行选通线 X 用于控制 T 5 和 T 6 两个 MOS 管；列选通线 Y 用于控制 T 7 和 T 8 两个 MOS 管
即上电后四个 MOS 管是截止的，即各自的源极和漏极不会导通
此时 T 3 T 4 各自的栅极都连接的是电源 VCC，可以认为是 5 V 高电平
因此 T 3 T 4 各自的源极和漏极之间是导通的，看成上拉电阻即可![[截屏2026-06-12 22.31.32.png]]
则撤销 b 点的高电平表示，由于 a 点链接到了 T 2 这个 MOS 管的栅极因此 a 点的高电平会使得T 2 管的源极和漏极之间导通，且 b 点通过 T 2 管连接到了图中有 GND 的符号表示接地，因此 b 点的电平被拉低至低电平0，![[截屏2026-06-12 22.44.54.png]] 
由于 b 点连接到了 T 1 这个 MOS 管的栅极因此 b 点的低电平会使得 T 1 管截止，即 T 1 管的源极和漏极之间不会导通![[截屏2026-06-12 22.47.23.png]]
![[截屏2026-06-12 22.48.09.png]]
由于电路是对称的
![[Pasted image 20260612224902.png]]
🧚‍♀️不考这么细；电容器？；涉及具体电路的一般都不考，除了 CPU 总线那些；模电只是讲讲 mos 管的性质，这部分是数字集成电路的内容，我正在学；结型场效应 mosfet；当三极管就行；中间四个管是双稳态触发器，外面四个是选通的，T 3 和 T 4 永远导通；上拉电阻：把一个引脚“默认拉到高电平（VCC）“”，但又不影响它被外部拉低![[截屏2026-06-12 22.48.55.png]]
SRAM 所有存储元在上电之后其初始状态是随机的，即各存储元存储的内容随机
#### 读操作
行选通 X 和列选通 Y 都是有效信号，即高电平 1
T 5 ～T 8 这四个 MOS 管各自的源极和漏极之间导通
`a` 点通过 T 5 和 T 7 与位线 I/O 连通了
`b` 点通过 T 6 和 T 8 与位线 I/O# 连通了
将两个位线的输出信号经过**差分放大器**后就可以根据电流方向的不同输出不同的数据信息了
![[截屏2026-06-12 22.51.16.png]]
读操作不会破坏原有的数据，即 a 点和 b 点的电平不会因为读操作发生改变（DRAM 的读操作是破坏性的，读操作之后必须立刻执行写操作恢复原本存储的数据）
🧚‍♀️细节细节，我算是弄懂怎么存储的了，前人都是怎么想出来的呀
#### 写操作
行选通 X 和列选通 Y 都要输入有效信号，即都是高电平，用 1 来表示
向位线 I/O 和位线 I/O# 分别输入低电平 0 和高电平 1，此时 a 点电平被拉低为低电平，b 点的电平无法被拉高（因为 T 2 任处于导通状态，b 任通过 T 2 管接地），当 a 的低电平使得 T 2 管截止之后 b 点的电平被拉高为高电平，b 点的高电平使得 T 1 管导通，此时 a 点通过 T 1 管接地保持可靠的低电平![[截屏2026-06-12 23.00.25.png]]
#### 信息的保持
当读操作或写操作完成之后撤掉行选通线 X 和列选通线 Y 的有效信号，变为无效信号 0
会导致 T 5 到 T 8 的四个 MOS 管截止![[截屏2026-06-12 23.01.37.png]] ![[截屏2026-06-12 23.02.00.png]]
#### 存储元特点
🧚‍♀️就记最后这点就行了，能和 DRAM 的存储元特性分开就行![[截屏2026-06-12 23.03.00.png]]

### 存储单元 🆚存储元🆚存储字
存储元是：
> **最基本的存储元件**，是最小存储元件。
   存储阵列由大量存储单元组成
   SRAM一个存储元大概是 6 个 MOS 管，能存 0 或 1

存储单元是：

> CPU 一次按地址访问的数据单位。

如果题目写：

```
64K × 8
```

那么：

- 存储单元数 = 64 K
- 每个存储单元 = 8 位
- 存储元数 = 64 K × 8

存储字是：

> 计算机（CPU）在进行读写操作时，**一次性作为一个整体进行存取和处理的数据单位**。


总结：
- **存储元**：最小单位，通常存 1 bit。
- **存储单元**：一个地址对应的内容，由若干存储元组成。
- 存储单元是“容器”，而存储字是这个容器里装的“内容”
- 通常情况下，每个内存地址对应的是一个存储单元（存放一个存储字），而不是单个存储元
- 一个存储字中包含的存储元（即二进制位）的个数，被称为**存储字长（Word Length）**。例如，在一台 64 位计算机中，它的存储字长通常是 64 位，这意味着它的一个存储字是由 64 个物理存储元构成的。
### 存储元扩展和存储阵列扩展
一个存储元只能存储一位二进制信息
实际 SRAM 存储芯片的存储体中往往包含多个存储元，所以需要进行存储元扩展形成存储阵列，甚至需要对存储阵列进行扩展最终构成 SRAM 存储芯片的存储体
🧚‍♀️有人问这几节课到底考不考，408 中考的是选择，占比小，就如那句话，408 学习周期学习所耗时间较长，并且成绩不一定跟付出的时间成正比，需要看考题趋势，若遍怪可以看看；不考电路的，考特点；但是讲了这个电路，可以理解为什么有这样的特点
#### 存储元扩展
上节课的存储元是典型的 6 MOS 管 SRAM 存储元
![[截屏2026-06-12 23.06.34.png]] ![[截屏2026-06-12 23.06.45.png]]
下面两个 MOS 管共享所以通常不算入一个 SRAM 存储元中

To 方便绘制存储元扩展形成的存储阵列，用存储元的符号进行表示屏蔽构成细节
![[截屏2026-06-12 23.07.41.png]]

👇 绘制由存储元扩展形成的存储阵列
1️⃣将每一行的 n 个存储元各自的行选通线并联在一起形成这一行的选通线![[截屏2026-06-12 23.09.32.png]]
2️⃣将每一列的 n 个存储元各自的位线并联在一起![[截屏2026-06-12 23.10.04.png]]
PS：⚠️每个存储元都有 D 和 $\overline{D}$ 两条位线，并且每一列的所有存储元都共享不算入单个存储元的两个 MOS 管，这两个 MOS 管由该列的列选通线控制，将每一列存储元并联后的位线 D 经过该列相应的 MOS 管并联在一起送入 I/O 电路中；![[截屏2026-06-13 16.09.55.png]] 同理将每一列存储元并联后的位线 $\overline{D}$ 经过该列相应的 MOS 管并连在一起送入 I/O 电路
![[截屏2026-06-13 16.10.07.png]]
包含 X 0 到 Xn-1 共 n 条行选通线；Y 0 到 Yn-1 共 n 条列选通线... 👇 ![[截屏2026-06-13 16.11.44.png]]
同一时刻只有一条行列选通线输出
![[截屏2026-06-13 16.16.38.png]]
例如：所有行选通线输出无效信号即低电平 0，此时所有存储元内部的 T 5 和 T 6 这两个 MOS 管截止；![[截屏2026-06-13 16.36.52.png]]所有列选通线输出无效信号，此时每一列所有存储元共享的两个 MOS 管截止![[截屏2026-06-13 16.36.46.png]]
假设读取该存储元所存储的一位二进制数据则需要该存储元所在行的行选通线输出有效信号，此时该行所有存储元内部的 T 5 和 T 6 两个 MOS 管导通；![[截屏2026-06-13 16.37.53.png]]
另外需要该存储元所在列的列选通线输出有效信号，此时该列存储元共享的 mos 管导通
![[截屏2026-06-14 11.57.58.png|200]]
此时该存储元所存储的内容就可以通过这列所有存储元并联在一起的位线 D 和位线 D # 输出到I/ O 电路，经过 I/O 电路输出该存储元所存储的一位二进制数据
![[截屏2026-06-14 12.00.43.png]]

🧚‍♀️感觉和双译码结构有点像

Q：如何一次访问多个存储元
🧚‍♀️将所有 x 或者 y 再并联一下；多个 io 口？可以理解为一片存储阵列只能访问一位二进制数据，但我可以同时访问多个存储阵列啊
![[截屏2026-06-14 12.03.11.png]]
#### 存储阵列扩展
由多个 SRAM存储元扩展的存储阵列一次只能访问一个存储元，即一位数据，所以可以使用多个存储阵列进行扩展-->一次访问多个存储元构成的存储字，例如下图👇一次可以访问 4 个存储元构成的存储字即存储字长为 4 位==>构成 SRAM 存储器的存储体

![[截屏2026-06-14 12.06.43.png]]
🧑‍🏫通过上述存储阵列扩展后形成的存储体，存储字长为 4 位，包含 $n^2$ 个存储字，即一次性可以输入输出 4 位二进制数据
⚠️不是 $n \times n$ 位，而是 $n \times n$ 个 4 位的存储字
🧚‍♀️要，重点，大题长出；数电内容；王道 86 页；变 3 d 了
1、存储字长，类比机器字长理解。该存储体可以一次输入/输出 4 位，则存储字长为 4 位。；2、存储字，就是一次输入/输出的一串二进制数据，在该存储体中，存储字的长度为 4 位，也就是存储字长。；如果本处最后两句没有理解，可以把这四个芯片摞在一起，在对应的 (i, i) 的一个存储元位置相当于一个重叠在一起的存储单元 (25/4/30)；意思是一次从四个芯片中各自读取一位，这四位构成了一个存储字为 4 位，有 $n*n$ 个这样的存储字；四个存储阵列即存放的一串二进制数有四位，一行可以存 n 串二进制数（因为每个阵列存放一位二进制数，一个阵列有 n 列），一共 n 行，所以一共能存 $n*n$ 串二进制数（一串二进制数就是一个存储字）；也就是位扩展，并联地址线、并联数据线，同时可输出 1 个字长为 4 位的数据，总共可以存取 NxN 个 4 位数据；不理解的可以联想一下之前讲过的双译码结构
### 存储器结构及其芯片实例
#### 存储体符号
![[截屏2026-06-14 12.19.58.png]] 
#### 存储器结构
假设 SRAM 存储器的存储体由 4 片 $64 \times 64$ 的存储阵列通过行选通线并联和列选通线并联扩展而成
在存储体中每个 $64 \times 64$ 的存储阵列包含有 $64 \times 64 = 4096$ 个存储元
每个存储元存储 1 位二进制数据
包含有 $X_0 \sim X_{63}$ 共 64 条行选通线， $Y_0 \sim Y_{63}$ 共 64 条列选通线
存储字长为 4 位，一次可存取 4 位二进制数据
![[截屏2026-06-14 12.25.02.png]] 🧚‍♀️存储字长为四位，不是有四片存储阵列的原因吗？跟行选通线和列选通线有什么关系呢？；可以理解为每次只能从一片存储阵列里存取一个二进制位，所以存取字长是四位；叠放了四片，每一片一次只能访问一个 bit；一次性可以同时选中这四个存储阵列的存储元了，一共四个存储元，那一个存储单元的存储字长不就是 4 了

🧑‍🏫存储体的各条行选通线应该和行地址译码器各条译码输出线相应连接
由于本例行选通线有 64 条，相应行地址译码器有 6 位地址输入 $2^6=64$；列是同理的，这种译码结构是本章用于地址译码的译码结构那节课中双译码结构![[截屏2026-06-14 12.34.49.png]]
在实际的 SRAM 存储器中译码器的各条译码输出线实际上不是直接连接到存储体的相应选通线上，而是通过驱动器后再与相应的选通线连接
目的：增强译码器的负载能力![[截屏2026-06-14 12.36.09.png]]
🧚‍♀️可以理解为，4 片一起选中某行某列，那就是选中四片中相同位置的“小方格”那就是 4 位了，所以一次能存取 4 位，存储字长也就是 4 位；注意无论是双译码结构还是单译码结构，地址输入线的总条数都是 12 条，视频结尾的题目里会涉及这一点；肯定啊一个是 12 次方另一个是 2 个 6 次方都是十二根；老师讲的内容和谭志虎主编的计算机组成原理微课版这本书重合度很高，可以买这本书

🧑‍🏫SRAM 存储阵列中存储元的构成细节![[截屏2026-06-14 12.37.13.png]]
行地址译码器的每条译码输出线都要同时驱动这一行上所有存储元的 T 5 T 6 两个 MOS 管
4 片存储阵列并发，每条译码输出线要驱动 $2 \times 64 \times 4 = 512$ 个 MOS 管，负载很大![[截屏2026-06-14 14.40.29.png]]
除译码器和驱动器之外，SRAM 存储器还包含 **I/O 电路**，处在存储单元与数据总线之间
读出时 4 对位线信号经 I/O 电路中各自的差分放大器放大后根据电流方向的不同输出 4 位二进制数据
写入时数据总线上的数据送入 I/O 电路再写入到相应的存储单元![[截屏2026-06-14 14.43.21.png]]
上述译码器、驱动器、I/O 电路需要在控制电路的控制下进行工作，控制电路包含两条非常重要的控制线，分别是片选线 CS 和写使能控制线 WE，都是低电平有效所有英文缩写上有上划线
🧚‍♀️Chip Select 和 Write Enable；write enabling 和 chip selection ![[截屏2026-06-14 14.51.17.png]]
$\overline{CS}$ 的作用：一块 SRAM 存储器芯片容量有限，满足不了大容量存储器的需求时可以使用多片 SRAM 存储器芯片进行扩展，对于扩展而来的存储器芯片进行访问时并不是所有 SRAM 存储器芯片都会同时工作，通过片选信号 $\overline{CS}$ 可很好解决 SRAM 存储器芯片的选择问题，只有片选信号有效的 SRAM 存储器芯片才能被访问
$\overline{WE}$ 的作用：存储器的读写操作需要由写使能控制线 $\overline{WE}$ 进行控制，$\overline{WE}$ 为**有效电平即低电平**时表明要对存储器进行写操作；$\overline{WE}$ 为无效电平即高电平时即可以从存储器读取信息
🧚‍♀️如灯泡一侧高电平，另一侧你使他高电平，就没有电压差，灯泡不工作。而另一侧你使他低电平，才有电压差……；we 规定是低电平有效，高电平就无效了

### SRAM 存储器芯片实例
#### Intel 2114
20 世纪 70 年代末期到 80 年代初期被广泛使用的👇![[截屏2026-06-14 15.25.41.png]]
![[截屏2026-06-14 15.22.27.png]]
行地址线共 6 条，列地址线共 4 条，是存储阵列 $64 \times 16$ 的二维结构；黄色的三角形是缓冲器的符号，主要功能是增强信号的驱动能力，相当于行驱动器和列驱动器的作用；紫色的三角形是三态门的符号，三态门的作用是控制 I/O 1～I/O 4 的 4 条数据线的传输方向
**写入控制**：当片选引脚 CS 输入有效电平 0（低电平有效）时该芯片被选中工作，假设进行写入操作则写使能引脚 WE 输入有效电平 0，上面与门输出 1 下面与门输出 0，与门输入引脚上的小圆圈可以简单认为是非门，上面的与门输出 1 传送到上方的 4 个三态门各自的控制器，导致 4 个三态门的输入与输出连通，下面的与门输出 0 传输到右侧的三态门各自的控制端会导致 4 个三态门的输入与输出断开输出为高阻态，可简单认为 4 条输出线与 I/O 线断开连接![[截屏2026-06-14 15.23.41.png]]
![[截屏2026-06-14 15.19.23.png]]
此时可以给 I/O 线输入要写入存储体的数据，==>CS 和 WE 都输入有效信号时是进行写入控制![[截屏2026-06-14 15.25.29.png]]
🧚‍♀️不会考具体芯片电路图，不过这部分知道三态门作用还是很好分析的；后面要画的，需要自己画；三态门记住高阻态和导通态就行了，导通态输出 0 或 1，高阻态近似断路；这个三态门的样式让我想起来我的世界的中继器，减法模式和比较模式；三态的意思是低电平、高电平、高阻态（无信号）；三态门不工作，电气特性一般可以直接等价为开路

**读取控制**：CS 输入有效电平则该芯片被选中工作，WE 输入无效电平即不进行写操作，上面与门输出的 0 传送到上方四个三态门各自的控制端会导致这 4 个三态门输入输出断开，下面与门的输出 1 传送到右侧的 4 个三态门各自的控制端，会导致 4 个三态门的输入和输出连通，此时内部 IO 电路与外部 IO 线连通![[截屏2026-06-14 15.29.43.png]]
🧚‍♀️神发明这玩意的是多懂电路，他简直就是在玩电路

🧑‍🏫Intel 2114 SRAM 芯片内部结构和原理如上，早已停产
#### 北京矽成半导体有限公司 ISSI 
由北京矽成半导体有限公司 ISSI 出品的 SRAM 芯片

![[截屏2026-06-14 15.30.36.png]] 🧚‍♀️对于嵌入式专业，这芯片好熟悉；图是嘉立创上面找的![[截屏2026-06-14 15.32.19.png
![[截屏2026-06-14 15.32.56.png]]
🧑‍🏫2006 年制作一款 ARM 开发板就采用了这款 SRAM 芯片，这么多年过去价格几乎没变，存储容量是 512 KB 即 0.5 MB (兆字节)

现在一条 8 GB 容量的 DDR 4 内存条价格甚至不足 100 元，是该 SRAM芯片价格 4 倍之多但是容量是该芯片的 16000 倍，证明 SRAM 存储器芯片比 DRAM 存储器芯片贵得多，做过嵌入式系统硬件开发的同学会有体会要为系统硬件设计选项芯片需要阅读芯片的数据手册 DataSheet 是必不可少的，通过数据手册了解芯片内部结构框图![[截屏2026-06-14 15.40.43.png]]
与 Intel 2114 结构相似，只是地址线、数据线、控制线有所增加
通过芯片真值表就可以掌握芯片的使用方式，即地址线、数据线、控制线之间的逻辑操作关系
![[截屏2026-06-14 15.41.27.png]]
⚠️每个型号的 SRAM 芯片尤其特点的读写周期特性，只有按照读写周期去访问 SRAM 芯片才能保证读写操作的正确性
![[截屏2026-06-14 15.42.39.png]]
例如这是该芯片的两种读操作的读周期时序图![[截屏2026-06-14 15.43.10.png]]
除此之外还要关注芯片的电源特性，例如最大工作电压、供电电流、工作温度范围（商业应用环境 or 工业应用环境）、直流电气特性（高低电平各自的范围）等
![[Pasted image 20260614154346.png]]
![[截屏2026-06-14 15.44.50.png]]
最后还要知道芯片的封装以便正确绘制 PCB
![[截屏2026-06-14 15.45.24.png]]
🧚‍♀️26 年 8 gddr 4 要 500 了；不能比，8 G DDR 4 需求量、太大了；二手的 70 块；就是说你读取要按 cpu 时钟周期来读，规范化；还是比较建议拿本计算机组成原理的教材；这些都是电子院学的；求占空比要用
## 习题
![[截屏2026-06-14 15.46.20.png]]