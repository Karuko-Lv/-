层次结构及分层-->系统复杂则分层
要求：底层封装接口给上层
🆚计算机网络也有分层思想
不同计算机系统分层没有统一标准![[截屏2026-06-01 21.23.33.png]]
## 六层计算机系统
第一层：数电中学习的逻辑门、寄存器等逻辑电路
第二层：微程序的基本思想是将**一条机器指令**编写成**一个微程序**；每个微程序包含若干条**微指令**-->一条机器指令包含多条微指令；每条微指令包含一条或多条**微操作**
🧑‍🏫在有微指令的系统中CPU内部有一个控制存储器，用于存放各种机器指令对应的微程序==>CPU执行机器指令会在**控制存储器**里寻找与**该机器指令对应的微程序**，找到后取出微程序中的微指令控制执行各个微操作从而完成机器指令的功能
第三层：指令集体系结构，定义一台具体的计算机可以执行的全部的机器指令的集合，定义具体的机器指令计算机应该执行什么操作以及所处理的操作数存放的位置以及操作数的类型等等...
🧑‍🏫一个机器语言程序是一个符合ISA固定的多条机器指令的序列；计算机**硬件执行**机器语言**程序**的过程即**执行**程序所包含每一条**机器指令**的过程
第四层：操作系统对计算机硬软件资源进行统一的管理和调度，方便用户使用计算机
操作系统设计人员要对机器的指令集和汇编语言很熟以及适合用于编写操作系统软件的高级语言e.gC
第五层：汇编语言层，设计人员一般是汇编语言程序设计人员，汇编语言是面向机器的语言，**每一条语句都与机器语言的某一条语句一一对应**，要求程序员对实际机器的内部组成和指令系统要熟悉
第六层：高级语言层，设计人员无需掌握机器底层技术细节![[截屏2026-06-01 21.47.15.png]]
上层最终用户使用这些应用程序解决各种应用问题
🧚‍♀️为什么操作系统还在汇编下面啊；我的理解是操作系统是硬件给软件提供的一个平台，所以也算作一层![[截屏2026-06-01 21.49.58.png]]
## 软硬件逻辑功能等价性
特定条件下软件实现的逻辑功能可以通过硬件电路实现
🧚‍♀️51单片机上连总线的很多功能都要用户自己实现，操作系统层也没有；比如显卡的矩阵计算；所以会出现各种游戏模拟器，用来实现游戏主机硬件的功能

软件层面实现逻辑功能：使用CPU执行程序
硬件层面实现逻辑功能：使用专用的硬件电路 e.g现场可编程门阵列 Field Programmable Gate Array或专用集成电路Application Specific Integrated Circuit
![[截屏2026-06-01 22.24.54.png]]
🧑‍🏫上世纪90年代VCD 1️⃣为在主频为33兆赫兹的计算机上播放VCD需要为计算机加一张VCD解码卡
2️⃣主频80兆赫兹的80486CPU时使用xing软件在不使用VCD解码卡的情况下可以播放VCD了只是打不到较高的帧率
 
🧚‍♀️FPGA也不能称为专用；微程序和硬布线；虚拟机软件，游戏主机模拟器软件……；路由和软路由；CAS操作；RAID和软RAID；虚拟内存算吗？

硬件电路可以并行执行操作
🧚‍♀️但是软件的实现需要通用逻辑的硬件吧？；留声机和音乐软件；集线器与拓展坞；这种等价性更多的是强调一个功能实现上的trade off，一个功能，是用什么部分实现去做更“值”，等价是"功能可互换"，不是"两者无关"，而且实现层面是软件单向依赖硬件
![[截屏2026-06-01 22.28.49.png]]
## 习题
![[截屏2026-06-01 22.29.22.png]]
C 、 B
🧚‍♀️D:上层对下层有依赖，下层对上层没有依赖，仅提供基础。
![[截屏2026-06-01 22.30.46.png]]
D C
[Gemini] **硬件并不是完全不能脱离软件运行，而是不能脱离“指令”运行**。软件就是这些指令的组织形式。
🧚‍♀️a选项汇编语言的执行效率高于高级语言程序吗； c应该是没有硬件汇编就不能使用了； 是的，通常越高级的语言效率越低；我看的资料说是等效不能是等价的；题3的c应该说对于计算机功能实现，不然硬件本身当然可以单独运行
## ‼️计算机基本工作原理
### 计算机硬件组成的细化
![[截屏2026-06-01 22.46.46.png]]
运算器的核心是ALU，除此至少还包含3个寄存器
1️⃣累加器ACC
2️⃣乘商寄存器MQ
3️⃣操作数寄存器X
#### 运算器结构框图
运算器进行运算时ALU ACC MQ和X各自的作用
1️⃣ALU执行“加”的时候 需要将被加数放入到累加器中，加数放到操作数寄存器中，ALU执行完成加法运算后累加的和保存在累加器ACC中
2️⃣ALU执行“减”的时候 首先将被减数送到累加器ACC中 减数放到操作数寄存器中，ALU执行完成减法运算后得到的差保存在累加器ACC中
3️⃣ALU执行“乘”的时候 被乘数要送到操作数寄存器X中，乘数送到乘商寄存器MQ中 ALU执行完成乘法之后得到的乘积高位会保存在累加器中，低位保存在乘商寄存器MQ中
🧚‍♀️保存的和会覆盖被加数吗；肯定覆盖；为什么这里的操作数寄存器在乘法运算的时候存储的是被乘数，而不是乘数呢？其他三个算法都存算式右边的值，为什么乘法是取左边的值呢？；25真题考了除法大题；后面学完乘法电路原理 你就懂了；其实这几个寄存器离得很近前两个几乎挨一起的，所以和一起才是整个乘商结果；其实这是汇编语言的内容；后面一章就会讲这几个寄存器的细节了；简单来说，操作数寄存器存储的是在执行算数运算时的“不变量”

![[截屏2026-06-01 22.53.59.png]]![[截屏2026-06-01 22.55.18.png]]
