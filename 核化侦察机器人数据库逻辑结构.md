## 🎯 1. 核心任务与数据总线
* **表 1: Missions (任务信息表)**
    * `MissionID (PK)`
    * `MissionName`
    * `StartTime` / `EndTime`
    * `LeadOperatorUserID (FK -> Users)`
    * `Status`
* **表 2: HistoricalDataLog (历史数据日志总表)**
    * `LogID (PK)`
    * `MissionID (FK -> Missions)`
    * `Timestamp`
    * (核心遥测数据: `Longitude`, `Latitude`, `GammaDoseRate_GyH`, `RobotSpeed_kmh`, `BatteryLevel_pct`, etc.)

## 🔬 2. 传感器与分析详情
* **表 9: SpectrumDataLog (能谱数据表)**
    * `SpectrumID (PK)`
    * `LogID (FK -> HistoricalDataLog)`
    * `SpectrumRawData`
* **表 10: SpectrumPeakLog (能谱峰位日志表)**
    * `PeakLogID (PK)`
    * `SpectrumID (FK -> SpectrumDataLog)`
    * `PeakChannel`
    * `PeakEnergy_keV`
* **表 8: AnalysisResultLog (分析结果日志表)**
    * `AnalysisID (PK)`
    * `LogID (FK -> HistoricalDataLog)`
    * `ResultName`
    * `Confidence_Match`
* **表 21: NuclideLibrary (核素库表) [配置]**
    * `NuclideName (PK)`
    * `EnergyPeak1_keV`

## 🛡️ 3. 设备与系统管理
* **表 6: Devices (机载设备主表) [配置]**
    * `DeviceID (PK)`
    * `DeviceName`
    * `DeviceType`
* **表 7: DeviceStatusLog (设备状态日志表)**
    * `StatusLogID (PK)`
    * `DeviceID (FK -> Devices)`
    * `Timestamp`
    * `Status`
* **表 22: DeviceSettings (设备参数配置表) [配置]**
    * `SettingKey (PK)`
    * `DeviceName`
    * `SettingValue`

## 🔐 4. 用户、权限与审计 (RBAC)
* **表 16: Users (用户表)**
    * `UserID (PK)`
    * `Username`
* **表 17: Roles (角色表)**
    * `RoleID (PK)`
    * `RoleName`
* **表 18: UserRoles (用户角色关联表)**
    * `UserID (FK -> Users)`
    * `RoleID (FK -> Roles)`
* **表 19: Permissions (权限定义表)**
    * `PermissionID (PK)`
    * `Description`
* **表 20: RolePermissions (角色权限关联表)**
    * `RoleID (FK -> Roles)`
    * `PermissionID (FK -> Permissions)`
* **表 4: UserActivityLog (用户活动审计表)**
    * `ActivityID (PK)`
    * `UserID (FK -> Users)`
    * `Timestamp`
    * `ActivityType`
* **表 5: SystemEventLog (系统事件日志表)**
    * `EventID (PK)`
    * `Timestamp`
    * `LogLevel`
    * `Message`

## 🚨 5. 告警与处置作业
* **表 3: AlarmLog (报警日志表)**
    * `AlarmID (PK)`
    * `MissionID (FK -> Missions)`
    * `Timestamp`
    * `AlarmType`
    * `Details`
* **表 11: DisposalOperationLog (处置作业操作日志表)**
    * `OperationID (PK)`
    * `MissionID (FK -> Missions)`
    * `DeviceID (FK -> Devices)`
    * `UserID (FK -> Users)`
* **表 12: DeconOperationDetails (洗消作业详情表)**
    * `DetailID (PK)`
    * `OperationID (FK -> DisposalOperationLog)`
    * `FlowRate_L_min`
* **表 13: WaterCannonOperationDetails (压制水炮详情表)**
    * `DetailID (PK)`
    * `OperationID (FK -> DisposalOperationLog)`
    * `FlowRate_L_min`

## 🗺️ 6. 媒体与地理标注
* **表 14: MediaLog (媒体文件日志表)**
    * `MediaID (PK)`
    * `MissionID (FK -> Missions)`
    * `Timestamp`
    * `MediaType`
    * `FilePath`
* **表 15: MapAnnotations (地图标注表)**
    * `AnnotationID (PK)`
    * `MissionID (FK -> Missions)`
    * `Timestamp`
    * `Longitude` / `Latitude`
    * `LabelText`
    * `CreatorUserID (FK -> Users)`