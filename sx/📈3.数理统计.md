1.  最大似然估计是根据一组样本的结果，来估计总体分布中的**未知参数 $\theta$**。
	1. “最大似然估计的准确度”怎么算？==>三个**评选标准** ，这些标准共同衡量了一个估计量的“准确度”
		1. 无偏性 (Unbiasedness) 
			- **含义：** 估计量的数学期望 $E(\hat{\theta})$ 是否等于真参数 $\theta$？
			- **理解：** 如果 $E(\hat{\theta}) = \theta$，就说它是无偏的。意思是，虽然我们这一次的估计 $\hat{\theta}$ 可能偏高或偏低，但如果我们反反复复抽样估计无数次，这些估计值的**平均值**正好就是那个真值。
			- **“准确”的体现：** 估计没有系统性的偏差。

# 数理统计全考点精要笔记

## 第一部分：大数定律与中心极限定理

### 1. 切比雪夫不等式 (Chebyshev's Inequality)

- **定理内容**：设随机变量 $X$ 具有数学期望 $E(X)=\mu$，方差 $D(X)=\sigma^2$，则对任意 $\varepsilon > 0$：$$P\{|X-E(X)| \ge \varepsilon\} \le \frac{D(X)}{\varepsilon^2}$$ 或者等价形式：$$P\{|X-E(X)| < \varepsilon\} \ge 1 - \frac{D(X)}{\varepsilon^2}$$ - 🔴 **[考点]**：常用于在分布未知但期望方差已知时，估算概率的上界或下界。
### 2. 依概率收敛

- **定义**：序列 $\{X_n\}$ 依概率收敛于常数 $a$，记为 $X_n \xrightarrow{P} a$，即：$$\lim_{n\to\infty} P\{|X_n - a| < \varepsilon\} = 1$$
### 3. 大数定律 (Laws of Large Numbers)

大数定律探讨的是随机变量序列的算术平均值 $\frac{1}{n}\sum X_i$ 收敛于其均值的期望。

1. **切比雪夫大数定律**
    - **条件**：$X_n$ 相互独立（或两两不相关），方差有共同上界 ($D(X_i) \le M$)。
    - **结论**：$\frac{1}{n}\sum_{k=1}^n X_k \xrightarrow{P} \frac{1}{n}\sum_{k=1}^n E(X_k)$。
2. **伯努利大数定律**
    - **场景**：$n$ 次独立重复试验，事件 $A$ 发生 $X_n$ 次（频率稳定性）。
    - **结论**：$\frac{X_n}{n} \xrightarrow{P} p$。
3. **辛钦大数定律 (Khinchin's Law)**
    
    - **条件**：$X_n$ **独立同分布 (i.i.d)**，且期望 $E(X_i)=\mu$ 存在。
        
    - **结论**：$\frac{1}{n}\sum_{i=1}^n X_i \xrightarrow{P} \mu$。
        
    - 🔴 **[考点]**：只要题目给出“独立同分布”且“期望存在”，即可直接使用辛钦大数定律判断收敛性。
        

### 4. 中心极限定理 (Central Limit Theorems)

研究随机变量之和在标准化后近似服从正态分布的规律。

1. **列维-林德伯格定理 (Levy-Lindberg)**
    
    - **条件**：$X_n$ **独立同分布**，期望 $\mu$ 和方差 $\sigma^2 > 0$ 存在。
        
    - **结论**：当 $n$ 充分大时，$\sum_{i=1}^n X_i$ 近似服从 $N(n\mu, n\sigma^2)$。标准化形式：
        
        $$\lim_{n\to\infty} P\left\{ \frac{\sum_{i=1}^n X_i - n\mu}{\sqrt{n}\sigma} \le x \right\} = \Phi(x)$$
2. **棣莫弗-拉普拉斯定理 (De Moivre-Laplace)**
    
    - **条件**：$X_n \sim B(n, p)$ (二项分布)。
        
    - **结论**：二项分布的极限分布是正态分布。
        
        $$\lim_{n\to\infty} P\left\{ \frac{X_n - np}{\sqrt{np(1-p)}} \le x \right\} = \Phi(x)$$
    - 🔴 **[考点]**：利用此定理计算二项分布在大样本下的概率近似值。
        

## 第二部分：数理统计的基本概念

### 1. 基础定义

- **总体**：研究对象的全体，对应一个随机变量 $X$。
    
- **简单随机样本**：$X_1, \dots, X_n$ 相互独立且与总体 $X$ 同分布。
    
- **统计量**：样本的不含未知参数的函数（如 $\overline{X}, S^2$）。
    

### 2. 常用统计量及其性质

1. **样本均值**：$\overline{X} = \frac{1}{n}\sum_{i=1}^n X_i$
    
    - 性质：$E(\overline{X}) = \mu, \quad D(\overline{X}) = \frac{\sigma^2}{n}$。
        
2. **样本方差**：$S^2 = \frac{1}{n-1}\sum_{i=1}^n (X_i - \overline{X})^2$
    
    - 🔴 **[考点]**：注意分母是 $n-1$。它是总体方差 $\sigma^2$ 的**无偏估计**，即 $E(S^2) = \sigma^2$。
        
    - 🔵 **[小众点/推导]**：$S^2$ 的推导利用了 $\sum (X_i - \overline{X})^2 = \sum X_i^2 - n\overline{X}^2$。
        
3. **k阶原点矩**：$A_k = \frac{1}{n}\sum_{i=1}^n X_i^k$。
    

### 3. 三大抽样分布 (The Three Major Distributions)

🔵 **[核心口诀]**：见到平方找卡方，两个卡方比是F，正态除卡方是t。

1. $\chi^2$ **分布 (卡方分布)**
    
    - **定义**：设 $X_i \sim N(0,1)$ 独立，则 $\sum_{i=1}^n X_i^2 \sim \chi^2(n)$。
        
    - **性质**：
        
        - **可加性**：若 $X \sim \chi^2(n_1), Y \sim \chi^2(n_2)$ 独立，则 $X+Y \sim \chi^2(n_1+n_2)$。
            
        - **期望方差**：$E(\chi^2) = n, \quad D(\chi^2) = 2n$。
            
2. $t$ **分布 (学生氏分布)**
    
    - **定义**：设 $X \sim N(0,1), Y \sim \chi^2(n)$ 独立，则 $T = \frac{X}{\sqrt{Y/n}} \sim t(n)$。
        
    - **性质**：密度函数关于 $y$ 轴对称。当 $n \to \infty$，近似于 $N(0,1)$。
        
    - 🔴 **[考点]**：$T^2 \sim F(1, n)$。
        
3. $F$ **分布**
    
    - **定义**：设 $U \sim \chi^2(n_1), V \sim \chi^2(n_2)$ 独立，则 $F = \frac{U/n_1}{V/n_2} \sim F(n_1, n_2)$。
        
    - **性质**：$F_{1-\alpha}(n_1, n_2) = \frac{1}{F_\alpha(n_2, n_1)}$（倒数关系）。
        

### 4. 正态总体的抽样分布 (核心考点)

#### (1) 单个正态总体 $N(\mu, \sigma^2)$

1. $\overline{X} \sim N(\mu, \frac{\sigma^2}{n})$，标准化后 $\frac{\overline{X}-\mu}{\sigma/\sqrt{n}} \sim N(0,1)$。
    
2. $\frac{(n-1)S^2}{\sigma^2} \sim \chi^2(n-1)$。
    
3. 🔴 **[重要性质]**：$\overline{X}$ **与** $S^2$ **相互独立**。
    
4. 构造 $t$ 统计量：
    
    $$\frac{\overline{X}-\mu}{S/\sqrt{n}} \sim t(n-1)$$
    
    _(解释：分子是正态，分母是_ $\sqrt{\chi^2/(n-1)}$_)_。
    

#### (2) 两个正态总体 $N(\mu_1, \sigma_1^2), N(\mu_2, \sigma_2^2)$

1. 均值差：$\overline{X} - \overline{Y} \sim N(\mu_1 - \mu_2, \frac{\sigma_1^2}{n_1} + \frac{\sigma_2^2}{n_2})$。
    
2. 方差比：
    
    $$\frac{S_1^2/\sigma_1^2}{S_2^2/\sigma_2^2} \sim F(n_1-1, n_2-1)$$
3. 🔵 **[复杂考点]**：当 $\sigma_1^2 = \sigma_2^2 = \sigma^2$ 但未知时，构造 $t$ 分布：
    
    $$\frac{(\overline{X}-\overline{Y}) - (\mu_1-\mu_2)}{S_w \sqrt{\frac{1}{n_1} + \frac{1}{n_2}}} \sim t(n_1+n_2-2)$$
    
    其中 $S_w^2 = \frac{(n_1-1)S_1^2 + (n_2-1)S_2^2}{n_1+n_2-2}$ (加权方差)。
    

## 第三部分：参数估计

### 1. 点估计 (Point Estimation)

#### (1) 矩估计法 (Moment Estimation)

- **原理**：用样本矩 $A_k$ 替换总体矩 $\mu_k$。
    
- **步骤**：
    
    1. 计算总体原点矩：$E(X), E(X^2)$ 等，含未知参数 $\theta$。
        
    2. 列方程：$E(X) = \overline{X}$。若有两个参数，增加 $E(X^2) = \frac{1}{n}\sum X_i^2$。
        
    3. 解方程得到 $\hat{\theta}$。
        

#### (2) 最大似然估计法 (MLE)

- **原理**：选取使样本出现概率最大的参数值。
    
- **步骤**：
    
    1. **写似然函数** $L(\theta)$：
        
        - 离散型：$L(\theta) = \prod_{i=1}^n P(X_i=x_i; \theta)$。
            
        - 连续型：$L(\theta) = \prod_{i=1}^n f(x_i; \theta)$。
            
    2. **取对数**：$\ln L(\theta)$（将乘积转化为求和，便于求导）。
        
    3. **求导并令为0**：$\frac{d \ln L}{d\theta} = 0$。
        
    4. **解方程**：驻点即为估计值。
        
    
    - 🔵 **[易错点]**：如果似然函数关于 $\theta$ 单调（导数恒不为0），MLE通常在参数范围的**端点**处取得（例如均匀分布 $U(0, \theta)$，$\hat{\theta} = \max(X_i)$）。
        

### 2. 估计量的评价标准

1. **无偏性**：$E(\hat{\theta}) = \theta$。
    
    - _常见结论_：$\overline{X}$ 是 $\mu$ 的无偏估计，$S^2$ 是 $\sigma^2$ 的无偏估计。
        
2. **有效性**：在无偏估计中，方差 $D(\hat{\theta})$ 越小越好。
    
3. **一致性 (相合性)**：样本量 $n \to \infty$ 时，$\hat{\theta}$ 依概率收敛于 $\theta$。
    
    - _注_：矩估计和MLE一般都是一致估计量。
        

### 3. 区间估计 (Interval Estimation)

核心思想是寻找**枢轴量 (Pivotal Quantity)**。

#### (1) 单个正态总体 $N(\mu, \sigma^2)$

|待估参数|条件|枢轴量分布|置信区间公式 (置信度 $1-\alpha$)|
|---|---|---|---|
|**均值** $\mu$|$\sigma^2$ 已知|$Z = \frac{\overline{X}-\mu}{\sigma/\sqrt{n}} \sim N(0,1)$|$\overline{X} \pm u_{\alpha/2}\frac{\sigma}{\sqrt{n}}$|
|**均值** $\mu$|$\sigma^2$ **未知**|$T = \frac{\overline{X}-\mu}{S/\sqrt{n}} \sim t(n-1)$|$\overline{X} \pm t_{\alpha/2}(n-1)\frac{S}{\sqrt{n}}$|
|**方差** $\sigma^2$|$\mu$ 未知|$\chi^2 = \frac{(n-1)S^2}{\sigma^2} \sim \chi^2(n-1)$|$\left[ \frac{(n-1)S^2}{\chi^2_{\alpha/2}(n-1)}, \frac{(n-1)S^2}{\chi^2_{1-\alpha/2}(n-1)} \right]$|

#### (2) 两个正态总体

- **均值差** $\mu_1 - \mu_2$ ($\sigma_1^2=\sigma_2^2$ 未知)：利用 $t(n_1+n_2-2)$ 分布。
    
- **方差比** $\sigma_1^2 / \sigma_2^2$ ($\mu$ 未知)：利用 $F(n_1-1, n_2-1)$ 分布。
    

## 第四部分：假设检验

### 1. 基本原理

- **原假设** $H_0$ vs **备择假设** $H_1$。通常将“维持现状”或“等号成立”的设为 $H_0$。
    
- **两类错误**：
    
    - **第一类错误 (弃真)**：$H_0$ 为真，却拒绝了 $H_0$。概率为 $\alpha$ (显著性水平)。
        
    - **第二类错误 (取伪)**：$H_0$ 为假，却接受了 $H_0$。概率为 $\beta$。
        
- **显著性检验**：只控制第一类错误 $\alpha$。
    

### 2. 正态总体的假设检验 (拒绝域法)

基本步骤：提出假设 $\to$ 构造统计量 $\to$ 确定拒绝域 $\to$ 计算判断。

#### (1) 均值检验 ($H_0: \mu = \mu_0$)

- $\sigma^2$ **已知 (Z检验)**：统计量 $U = \frac{\overline{X}-\mu_0}{\sigma/\sqrt{n}}$。拒绝域 $|u| \ge u_{\alpha/2}$。
    
- $\sigma^2$ **未知 (t检验)**：统计量 $T = \frac{\overline{X}-\mu_0}{S/\sqrt{n}}$。拒绝域 $|t| \ge t_{\alpha/2}(n-1)$。
    

#### (2) 方差检验 ($H_0: \sigma^2 = \sigma_0^2$)

- $\chi^2$ **检验**：统计量 $\chi^2 = \frac{(n-1)S^2}{\sigma_0^2}$。
    
    - 拒绝域 (双侧)：$\chi^2 \le \chi^2_{1-\alpha/2}$ 或 $\chi^2 \ge \chi^2_{\alpha/2}$。
        

### 3. 单侧检验 vs 双侧检验

- **双侧**：$H_1: \mu \ne \mu_0$。拒绝域在两端，分位点用 $\alpha/2$。
    
- **单侧**：
    
    - $H_1: \mu > \mu_0$ (右侧检验)，拒绝域在右端 ($> u_\alpha$ 或 $t_\alpha$)。
        
    - $H_1: \mu < \mu_0$ (左侧检验)，拒绝域在左端 ($< -u_\alpha$ 或 $-t_\alpha$)。
        

### 4. p值法 (概念)

- 若 $p < \alpha$，拒绝 $H_0$。
    

## 🔵 补充：小众点与解题技巧 (Derived from exercises)

1. **区间估计与假设检验的关系**：一个参数的置信水平为 $1-\alpha$ 的置信区间，恰好对应显著性水平为 $\alpha$ 的双侧假设检验的接受域。如果 $\mu_0$ 在置信区间内，接受 $H_0$；否则拒绝。
    
2. $\chi^2$ **分布的构成**：只有当 $X_i \sim N(0,1)$ 时，$\sum X_i^2 \sim \chi^2$。如果 $X_i \sim N(0, \sigma^2)$，则 $\sum (X_i/\sigma)^2 \sim \chi^2$。**见到平方和，先想办法标准化，再找卡方分布。**
    
3. **独立性判断**：正态总体中，线性组合的独立性通常取决于协方差是否为0。特别地，$\overline{X}$ 与 $X_i - \overline{X}$ 是独立的。
    
4. **自由度识别**：
    
    - $\sum_{i=1}^n (X_i - \mu)^2 / \sigma^2 \sim \chi^2(n)$ (已知 $\mu$)。
        
    - $\sum_{i=1}^n (X_i - \overline{X})^2 / \sigma^2 \sim \chi^2(n-1)$ (未知 $\mu$，用 $\overline{X}$ 代替，自由度减1)。
        
5. **F分布的倒数性质**：在做方差比的区间估计时，常用 $F_{1-\alpha/2}(n_1, n_2) = 1 / F_{\alpha/2}(n_2, n_1)$ 来处理不等式的左边。