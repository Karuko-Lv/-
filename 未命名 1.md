```
graph TB
    %% 样式定义
    classDef hub fill:#fff9c4,stroke:#fbc02d,stroke-width:2px
    classDef sensor fill:#e1bee7,stroke:#8e24aa,stroke-width:2px
    classDef actuator fill:#ffccbc,stroke:#d84315,stroke-width:2px
    classDef bus fill:#ffffff,stroke:#999,stroke-dasharray: 5 5

    %% 核心节点
    ControlBox[<b>上装控制盒</b><br>(数据汇聚与转发层)]:::hub

    %% 传感器节点 (任务载荷执行层)
    subgraph Sensors [侦察探测载荷]
        direction TB
        Gamma[<b>γ能谱分析探头</b><br>(能谱数据)]:::sensor
        Neutron[<b>中子/γ剂量率仪</b><br>(剂量率数值)]:::sensor
        Raman[<b>固/液态检测仪</b><br>(物质识别结果)]:::sensor
        Gas[<b>工业毒气报警器</b><br>(气体浓度)]:::sensor
        Agent[<b>毒剂报警器</b><br>(毒剂类型)]:::sensor
    end

    %% 执行机构节点
    subgraph Actuators [处置执行载荷]
        WashSys[<b>洗消控制单元</b><br>(泵阀状态/控制)]:::actuator
    end
    
    %% 接口连接 (注明物理接口与协议)
    ControlBox <==>|<b>内部接口 01</b><br>Ethernet (TCP/IP)| Gamma
    ControlBox <==>|<b>内部接口 02</b><br>RS232 (自定义协议)| Neutron
    ControlBox <==>|<b>内部接口 03</b><br>Ethernet (TCP/IP)| Raman
    ControlBox <==>|<b>内部接口 04</b><br>RS485 (Modbus RTU)| Gas
    ControlBox <==>|<b>内部接口 05</b><br>RS485 (Modbus RTU)| Agent
    ControlBox <==>|<b>内部接口 06</b><br>CAN Bus (CAN 2.0B)| WashSys
``` 
