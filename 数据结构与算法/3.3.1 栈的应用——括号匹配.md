Leetcode 第 20 题[https://leetcode.cn/problems/valid-parentheses/?envType=study-plan-v2&envId=top-100-liked]

思路：遇到左括号压入栈中，遇到右括号将栈顶的左括号弹出，检查弹出的是否匹配
## 常见情形
1️⃣成功匹配
![[截屏2026-04-16 21.07.31.png]] 2️⃣左右不匹配![[截屏2026-04-16 21.08.07.png]] 3️⃣右括号单身，没有左括号与之匹配
![[截屏2026-04-16 21.08.45.png]]
4️⃣左括号单身，栈非空，并不存在与左括号相匹配的右括号![[截屏2026-04-16 21.11.00.png]]
「PS」扫描完所有括号还要检查栈是否为空，非空则失败
## 算法详细过程
从左往右扫描括号![[截屏2026-04-16 21.13.06.png]]
![[截屏2026-04-16 21.13.51.png]] 🧚‍♀️有些括号虽然单身但是好歹相亲过，还有些括号相亲都没有过

## 算法实现（代码）
```cpp
#define MaxSize 10//定义栈中元素的最大个数
typedef struct{
	char data[MaxSize];
	int top;
}SqStack;
//初始化栈
void InitStack(SqStack &S){
	S.top = -1;//top指针指向栈顶元素
}
//判断栈是否为空
bool StackEmpty(SqStack &S){
	if(S.top==-1)
		return true;
	else
		return false;
}
//新元素入栈
bool Push(SqStack &S,char x){
	//判断栈是否满了
	if(S.top==MaxSize)
		return false;
	S.data[++S.top]=x;
	return true;
}
//栈顶元素出栈
bool Pop(SqStack &S,char &x){
	if(S.top==-1)
		return false;
	S.data[S.top--]=x;
	return true;
}
bool bracketCheck(char str[],int length){
	SqStack S;//声明一个栈
	InitStack(S);
	for(int i = 0;i<length;i++){//1️⃣扫描括号元素
		if(str[i]=='(' || str[i]=='['||str[i]=="{"){//左括号入栈
			Push(S,str[i]);
		}else{
			if(StackEmpty(S))//右括号单身
				return false;
			else{
				char topElem;
				Pop(S,topElem);
				if(topElem != '(' && str[i] == ')')
					return false;//检查完所有的错误情况就是对的了
				else if(topElem != '[' && str[i] == ']')
					return false;
				else if(topElem != '{' && str[i] == '}')
					return false;
			}
		}
	}
	return StackEmpty(S);//检查完所有括号如果栈是空的就是匹配成功了✅
}
```
❌错误
1️⃣ `top` 永远指向当前栈顶元素的真实下标，`MaxSize` 是 10，数组的合法下标是 `0 ~ 9`
修改为
```cpp
bool Push(SqStack &S, char x){ // 判断栈是否满了 
	if(S.top == MaxSize - 1) // 【修改处】：改为 MaxSize - 1 
		return false; 
	S.data[++S.top] = x; 
	return true; 
}
```
2️⃣ `Pop` 函数中的赋值方向反了
`Pop` 函数的目的是把栈顶的元素**取出来**，并赋值给引用参数 `x`。但在你的代码中：`S.data[S.top--] = x;`，这表示把外面传进来的 `x`（可能是一个未初始化的垃圾值）写到了栈里面，这完全起不到出栈读取数据的作用，并且破坏了栈内数据。
3️⃣`bracketCheck` 函数中的字符语法错误
在判断左括号的 `if` 语句中，你写了 `str[i] == "{"`。在 C/C++ 中，单引号 `'{'` 表示单个字符（`char`），而双引号 `"{ "` 表示字符串（本质是 `const char*` 指针）。`str[i]` 是一个字符，它不能和一个字符串指针去进行 `==` 比较，这里会导致编译报错。
Gemini
```cpp
#define MaxSize 10 //定义栈中元素的最大个数

typedef struct{
	char data[MaxSize];
	int top;
}SqStack;

//初始化栈
void InitStack(SqStack &S){
	S.top = -1; //top指针指向栈顶元素
}

//判断栈是否为空
bool StackEmpty(SqStack &S){
	if(S.top == -1)
		return true;
	else
		return false;
}

//新元素入栈
bool Push(SqStack &S, char x){
	// 修复1：栈满的判断条件应为 MaxSize - 1，防止数组越界
	if(S.top == MaxSize - 1)
		return false;
	S.data[++S.top] = x;
	return true;
}

//栈顶元素出栈
bool Pop(SqStack &S, char &x){
	if(S.top == -1)
		return false;
	// 修复2：赋值方向必须是从栈中取出数据赋值给 x
	x = S.data[S.top--]; 
	return true;
}

bool bracketCheck(char str[], int length){
	SqStack S; //声明一个栈
	InitStack(S);
	for(int i = 0; i < length; i++){ //1️⃣扫描括号元素
		// 修复3：C/C++ 中字符必须使用单引号，将 "{" 改为 '{'
		if(str[i] == '(' || str[i] == '[' || str[i] == '{'){ //左括号入栈
			Push(S, str[i]);
		}else{
			if(StackEmpty(S))
				return false;
			else{
				char topElem;
				Pop(S, topElem);
				if(topElem != '(' && str[i] == ')')
					return false; //检查完所有的错误情况就是对的了
				else if(topElem != '[' && str[i] == ']')
					return false;
				else if(topElem != '{' && str[i] == '}')
					return false;
			}
		}
	}
	return StackEmpty(S); //检查完所有括号如果栈是空的就是匹配成功了✅
}
```
「PS」优化算法
1️⃣括号成功匹配的字符串一定是偶数个的
```cpp
if(length%2==1)
	return false;
```
2️⃣使用哈希表表示配对：C++ 标准模板库（STL）中的 **`unordered_map`（无序映射）**，它的底层数据结构是**哈希表（Hash Table）**
可以将
```cpp
if(topElem != '(' && str[i] == ')') 
	return false; 
else if(topElem != '[' && str[i] == ']') 
	return false;
else if(topElem != '{' && str[i] == '}') 
	return false;
```
替换成
```cpp
// 假设 str[i] 是右括号 ']'，pairs[']'] 就会自动帮你查出它对应的左括号应该是 '['
if (pairs[str[i]] != topElem) {
    return false; // 如果查出来的左括号，和栈顶弹出的左括号不一样，直接报错
}
```

![[截屏2026-04-16 22.39.53.png]]
「PS」实际中为了防止溢出用链栈，考试可以用顺序栈更简单也没问题
🧚‍♀️调接口一定拿不了高分满分，但可以骗分，可运行代码给基础分；伪代码要注意不要出现二义性，只要没有二义性，全是文字都没事；用 swich case 更方便，更好理解；在 for 循环体里初始化变量 topElem 是有问题的吧，应该在 for 循环之前初始化