队列是单链表的阉割版
队列中由于每次插入元素都只能在表尾的位置插入，因此可以用专门的尾指针指向最后一个结点
出队：从头结点找到第一个数据结点，将其删除即可
==>声明队列时即需要保存对首指针也需要保存队尾指针
![[截屏2026-04-14 22.04.55.png]]
## 初始化（带头结点）
![[截屏2026-04-14 22.18.55.png]]
```cpp
typedef struct LinkNode{
	int data;
	struct LinkNode *next;
}LinkNode;

typedef struct{
	LinkNode *front,*rear;
}LinkQueue;

void InitQueue(LinkQueue &Q){
	//初始化 front、rear指向头结点
	Q.front = Q.rear = new LinkNode();
	Q.front->next = nullptr;
}

void testLinkQueue(){
	LinkQueue Q;//声明一个队列 
	InitQueue(Q);//初始化队列
}
```
🧚‍♀️尾指针的循环链表比较好
「PS」有自引用所以需要 `typedef struct [结构体真名] { 成员变量... } [类型别名];`；没有自引用可以偷懒，不写“结构体真名”，直接让编译器创建一个匿名的结构体，然后立刻用 `typedef` 给它贴上一个叫 `LinkQueue` 的别名标签

### 判断是否为空
```cpp
bool isEmpty(LinkQueue Q){
	if(Q.front == Q.rear)
		return true;
	else
		return false;
}
```

```cpp
bool isEmpty(LinkQueue Q){
	if(Q.front == nullptr)
		return true;
	else
		return false;
}
```
头尾指针是否指向同一个结点；或者头结点的 `next` 指针是否指向 `null`

## 初始化（不带头结点）
```cpp
void InitQueue(LinkQueue &Q){
	Q.front = nullptr;
	Q.rear = nullptr;
}
```
### 是否为空
```cpp
bool IsEmpty(LinkQueue Q){
	if(Q.front == nullptr)
		return true;
	else
		return false;
}
```

```cpp
bool IsEmpty(LinkQueue Q){
	if(Q.rear == nullptr)
		return true;
	else
		return false;
}
```

## ru du