**递归算法**即递归函数调用自身的过程，递归的过程背后是函数调用的过程
## 函数调用背后的过程
![[截屏2026-04-17 17.14.07.png]]
当 func 2 执行结束后会返回 func 1，接着执行后面的代码；再等 func 1 执行结束之后又会返回 main 函数执行后续代码
==>**特性**：最后被调用的函数最先执行（LIFO）
实际任何一段代码任何一个程序运行之前系统都会开辟一个函数调用栈，用栈保存**各个函数在调用过程中必须保存的一些信息**：![[截屏2026-04-17 17.18.10.png]]
1️⃣调用返回地址（调用 `func 1` 函数的时候把 ` func 1` 执行结束之后应该执行的代码存储地址压到栈中）
2️⃣实参（函数调用的参数会放到栈中）
3️⃣局部变量
	「PS」由上图的栈可以看出 `main` 函数中的 `a`、`b` 变量数据对应的是 main：中的 ` a `、` b ` 两份数据，func 1 中修改 ab 的值影响不到 main 函数的 ` a ` 、` b ` 值，除了被调用函数的实参之外函数当中定义的局部变量 ` x ` 也是放在栈当中的; 之后 `func 1` 会调用 `func 2`，同样记录 `func 2` 执行之后应该回到哪一句继续往后执行，即将 `x=x+10086` 的存放地址给放到 ` func 2 ` 的栈里面，和刚才一样存放实参和局部变量，`func2` 执行完了之后就可以将与它相关的信息弹出栈，释放了 ` func 2` 这一片内存空间
函数调用栈其实本质上是内存中的某一片区域，某一片存储空间
🧚‍♀️递归内存爆炸！；Jvm 方法存在虚拟机栈；递归导致栈溢出; new 对象是堆内存；string str 是栈内存
**函数调用背后需要一个栈来支持**

程序从 main 函数开始，但是程序在经过编译之后编译器会在 main 函数之前加一些其他的代码，将一些不知道信息压入栈底，不需要普通程序员关心
🧚‍♀️这里指的 main 函数之前所执行的语句是预处理语句中的相关代码；main 函数之前的是宏操作
![[截屏2026-04-17 17.55.49.png]]
IDE 调试可以看到函数调用栈中保存了哪些信息，这里没有显示返回地址
## 递归算法
适合用递归算法的问题的特点：可以转换成属性相同但是规模较小的问题![[截屏2026-04-17 17.59.35.png]]
🧚‍♀️递归多了容易栈溢出；空间换时间；递归用不好会产生大量重复的计算；动态规划是用空间换时间；转 dp 降低时间复杂度；~={red}❌==>不要用栈的深度去算时间。计算递归的时间复杂度，一定要画出**递归树**，算一算这棵树上到底挂了多少个“节点”（总调用次数），再乘以每个节点内部执行代码的时间=~; 递归的空间复杂度就是栈的深度;递归算法可以被动态规划代替
### 求阶乘
![[截屏2026-04-17 19.18.24.png]]
问题规模逐渐收敛逐渐减小
**缺点：** 如果递归层数太多有可能会导致栈溢出，由于内存资源有限，包括系统开辟的函数调用栈都是有存储上限的
==>递归会带来空间复杂度升高，递归层数越多相应空间复杂度越高
🧚‍♀️难怪后面二叉树，老师之前又是叫我们用栈实现，又是叫我们用递归实现；阶乘的递增速度比指数还快；递归有上限，使用需谨慎；一般一个线程就几 mB 的栈区，压个几千层差不多了-->买电脑常说“我有 16 GB 的内存”, 6 GB 的大内存（主要叫做**堆区 Heap**）是给所有程序共享的，用来存大文件、大数组的;运行一段代码时，操作系统会给这条正在执行的执行流（**线程 Thread**）分配一块极其私密、小巧的专属内存，这就叫**栈区（Stack）** 操作系统里同时跑着几百上千个线程。如果每个线程都分几个 G 的栈，电脑当场就死机了; Java 开发中，默认的线程栈大小通常只有 **1 MB**; 在 Linux 下跑 C/C++ 程序，默认的栈大小通常是 **8 MB**; 在 Windows 下，默认栈大小通常也是 **1 MB**。
### 求斐波那契数列
![[截屏2026-04-17 19.38.24.png]]
「PS」 `Fib (2)` 被计算了两次， `Fib (1)` 和 `Fib (0)` 也同样被重复了多次 
🧚‍♀️递归算法和循环算法都是能替换的，时间换空间罢了；这应该是后续遍历；这就是 DFS 深度优先搜索；先序遍历
### 递归算法改成非递归
由于递归算法本质是栈实现的，如果需要把递归算法改造成非递归的实现方式完全可以自己定义一个栈，用类似的思想实现
**作用**：把递归改成普通的 `for/while` 循环，就不需要压栈了。如果算法本身非常复杂（比如树的遍历），必须用到“先进后出”的逻辑，我们可以在代码里手动创建一个 `Stack`（栈）对象。
**关键区别**：代码里手动 `new` 出来的 `Stack` 对象，是存放在计算机的**堆区（Heap）**里的！堆区有多大？你电脑有 16 GB 内存，它就能用十几 GB。这相当于把你的战场从“小胡同”转移到了“大平原”，再深的数据也能装得下。

## 递归的缺点
**递归虽好，但绝不能用来处理深层级的大规模数据。**

- **安全的场景：** 比如树的遍历。一棵包含 100 万个节点的平衡二叉树，它的深度只有 $\log_2(1,000,000) \approx 20$ 层。栈里才压 20 个盘子，非常安全。
- **危险的场景：** 比如用递归去遍历一个长度为 100,000 的链表。10 万次调用会直接把栈撑爆。
