安装和配置Git本地环境、在编译器中设置Git路径、配置用户信息（用户名和邮箱）、初始化本地仓库、添加远程仓库地址、配置.gitignore文件、设置SSH密钥或HTTPS认证、配置提交模板和代码格式化规则、在编译器中配置Git插件或内置功能、测试Git连接和基本操作（add、commit、push、pull）。
1. 
	- **`git pull` (拉取)**：相当于“**下载 + 合并**”。把 GitHub 上的最新代码拿下来，塞进你本地的文件夹里。
    
	- **`git push` (推送)**：才是真正的“**上传**”。把本地的代码发给 GitHub 存起来。