| **模块**                  | **核心内容**                                                   | **为什么算法题需要它**               |
| ----------------------- | ---------------------------------------------------------- | --------------------------- |
| **基础语法**                | 变量类型 (`int`, `bool`, `string`)、流程控制 (`if`, `for`, `while`) | 构建逻辑的最基本砖块。🧱               |
| **输入输出 (I/O)**          | `cin` (输入), `cout` (输出), `getline`                         | 算法题通常需要你从标准输入读取数据并打印结果。📥   |
| **容器 (STL Containers)** | `vector` (动态数组), `string` (字符串处理)                          | 存储小朋友的身高、处理文本等。📦           |
| **常用算法库**               | `sort`, `swap`, `max/min`                                  | 题目要求的“交换”、“排序”等操作都有现成工具。🛠️ |
| **逻辑运算技巧**              | 异或 (`^`)、布尔逻辑 (`!=`, `&&`, `                               |                             |

